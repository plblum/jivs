{
  "name": "@plblum/jivs-engine",
  "version": "0.9.0",
  "description": "The workhorse of validation. Classes without UI that just take in values and output validation results. This lib is completely standalone from DOM, HTML, and Web APIs. The Suite contains support for UI aspects of validation too.",
  "displayName": "JavaScript Input Validation Service",
  "license": "MIT",
  "main": "build/src/index",
  "typings": "build/types/src/index",
  "scripts": {
    "compile": "tsc",
    "test": "jest"
  },

  "eslintConfig": {
    "env": {
      "browser": true,
      "node": true,
      "jest": true
    }
  }
}
