import { jest } from '@jest/globals';
import { ConditionFactory } from "../../src/Conditions/ConditionFactory";
import { JivsServices } from "../../src/Services/JivsServices";
import { IValueHost, ValueHostConfig, ValueHostInstanceState } from "../../src/Interfaces/ValueHost";
import { MockValueHostsManager, MockJivsServices } from "../TestSupport/mocks";
import { ModelValidatorsValueHost, ModelValidatorsValueHostName } from '../../src/ValueHosts/ModelValidatorsValueHost';
import { ValueHostName } from '../../src/DataTypes/BasicTypes';
import { IFieldValueHost, FieldValueHostConfig, FieldValueHostInstanceState } from '../../src/Interfaces/FieldValueHost';
import { IssueFound, ValidationSeverity, ValidationState, ValidateOptions, ValidationStatus, ValueHostValidateResult } from '../../src/Interfaces/Validation';
import { IJivsServices } from '../../src/Interfaces/JivsServices';
import {
    Behaviors,
    IValueHostsManager,
    IValueHostsManagerAccessor,
    IValueHostsManagerCallbacks, StateContainer, ValueHostsManagerConfig, ValueHostsManagerInstanceState, createBehaviors, toIValueHostsManager,
    toIValueHostsManagerAccessor,
    toIValueHostsManagerCallbacks
}
    from '../../src/Interfaces/ValueHostsManager';
import { ValueHostType } from "../../src/Interfaces/ValueHostFactory";
import { ValueHostsManager } from "../../src/Validation/ValueHostsManager";
import { createJivsServicesForTesting } from '../../src/Support/createJivsServicesForTesting';
import { ConditionCategory, ConditionEvaluateResult } from "../../src/Interfaces/Conditions";
import { IValidatableValueHostBase, ValueHostValidationState, ValueHostValidationStateChangedHandler } from "../../src/Interfaces/ValidatableValueHostBase";
import {
    AlwaysMatchesConditionType, NeverMatchesConditionType, IsUndeterminedConditionType, UserSuppliedResultConditionConfig,
    UserSuppliedResultCondition, UserSuppliedResultConditionType,
    NeverMatchesConditionType2
} from "../../src/Support/conditionsForTesting";
import { IValidatorsValueHostBase } from "../../src/Interfaces/ValidatorsValueHostBase";
import { IValueHostAccessor } from "../../src/Interfaces/ValueHostAccessor";
import { CalcValueHostConfig, ICalcValueHost } from "../../src/Interfaces/CalcValueHost";
import { IStaticValueHost, StaticValueHostConfig, StaticValueHostInstanceState } from "../../src/Interfaces/StaticValueHost";
import { ConditionType } from '../../src/Conditions/ConditionTypes';
import { FieldValueHost, FieldValueHostGenerator } from '../../src/ValueHosts/FieldValueHost';
import { RegExpConditionConfig, RegExpCondition, EqualToValueConditionConfig, EqualToValueCondition } from '../../src/Conditions/ConcreteConditions';
import { LookupKey } from '../../src/DataTypes/LookupKeys';
import { ValueHostFactory } from '../../src/ValueHosts/ValueHostFactory';
import { deepClone } from '../../src/Utilities/Utilities';
import { ValidatableValueHostBase } from '../../src/ValueHosts/ValidatableValueHostBase';
import { StaticValueHost, StaticValueHostGenerator } from '../../src/ValueHosts/StaticValueHost';
import { CalcValueHost } from '../../src/ValueHosts/CalcValueHost';
import {
    TestValidatableValueHost,
    addTestValidatableValueHostGeneratorToServices,
    createValidatableValueHostBaseConfig
} from '../TestSupport/TestValidatableValueHost';
import { CapturingLogger } from '../../src/Support/CapturingLogger';
import { LoggingLevel } from '../../src/Interfaces/LoggerService';
import { ValueHostAccessor } from '../../src/ValueHosts/ValueHostAccessor';
import { IValueHostResolver, toIValueHostResolver } from '../../src/Interfaces/ValueHostResolver';
import { IDisposable } from '../../src/Interfaces/General_Purpose';
import { createStateContainer, createCapturedStateAsString } from '../TestSupport/utilities';
import { Publicify_ValueHostsManager } from '../TestSupport/Publicify_classes';


class Publicify_ValueHostsManagerWithTestValidatableValueHost extends Publicify_ValueHostsManager {
    public createModelValidatorsValueHostCallCount = 0;

    protected override createModelValidatorsValueHost(): IValidatorsValueHostBase {
        this.createModelValidatorsValueHostCallCount++;

        let existing = this.getValueHost(ModelValidatorsValueHostName);
        if (existing)
            return existing as IValidatorsValueHostBase;

        return this.addValueHost(
            {
                ...createValidatableValueHostBaseConfig(999),
                name: ModelValidatorsValueHostName,
                label: '*'
            },
            null
        ) as IValidatorsValueHostBase;
    }
}

function setupValueHostsManagerForAddExternalIssueFoundTests(
    configs?: Array<ValueHostConfig> | null
): {
    services: IJivsServices,
    valueHostsManager: Publicify_ValueHostsManagerWithTestValidatableValueHost
} {
    let services = createJivsServicesForTesting({ logger: 'capturing', loggerLevel: LoggingLevel.Debug });
    services.autoGenerateDataTypeCheckService.enabled = false;
    services.dataTypeParserService.enabled = false;
    addTestValidatableValueHostGeneratorToServices(services);

    let valueHostsManager = new Publicify_ValueHostsManagerWithTestValidatableValueHost({
        services: services,
        valueHostConfigs: configs ?? []
    });

    return {
        services,
        valueHostsManager
    };
}

//  constructor(setup: ValueHostsManagerConfig)
describe('constructor and initial property values', () => {
    test('No configs (empty array), an empty state and no callback', () => {
        let testItem: Publicify_ValueHostsManager | null = null;
        let services = new MockJivsServices(false, false);
        expect(() => testItem = new Publicify_ValueHostsManager({ services: services, valueHostConfigs: [] })).not.toThrow();
        expect(testItem!.services).toBe(services);

        expect(testItem!.publicify_valueHosts.size).toBe(0);
        expect(testItem!.publicify_valueHostConfigs.size).toBe(0);
        expect(testItem!.publicify_InstanceState).not.toBeNull();
        expect(testItem!.publicify_InstanceState.stateChangeCounter).toBe(0);
        expect(testItem!.onValidationStateChanged).toBeNull();
        expect(testItem!.onValueHostValidationStateChanged).toBeNull();
        expect(testItem!.onValueChanged).toBeNull();
        expect(testItem!.onTextValueChanged).toBeNull();
        expect(testItem!.onConfigChanged).toBeNull();
        let expectedBehaviors: Behaviors = createBehaviors(services);
        expect(testItem!.behaviors).toEqual(expectedBehaviors);
    });
    test('null setup parameter throws', () => {
        let testItem: Publicify_ValueHostsManager | null = null;

        expect(() => testItem = new Publicify_ValueHostsManager(null!)).toThrow(/config/);

    });
    // with config.behaviors initialized when passed into constructor
    test('Config.behaviors initialized when passed into constructor', () =>
    {
        let behaviors: Behaviors = {
            activeCultureId: 'en-US',
            disableFormattingOnValueChange: true,
            disableParsingOnValueChange: false
        };
        let config: ValueHostsManagerConfig = {
            services: new MockJivsServices(false, false),
            valueHostConfigs: [],
            behaviors: behaviors
        };
        let testItem: Publicify_ValueHostsManager | null = null;
        expect(() => testItem = new Publicify_ValueHostsManager(config)).not.toThrow();
        expect(testItem!.behaviors).toEqual(behaviors);
    });

    test('Config for 1 ValueHost supplied. Other parameters are null', () => {
        let configs: Array<ValueHostConfig> = [{
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        }];
        let testItem: Publicify_ValueHostsManager | null = null;
        let services = new MockJivsServices(false, false);
        expect(() => testItem = new Publicify_ValueHostsManager({ services: services, valueHostConfigs: configs })).not.toThrow();
        expect(testItem!.services).toBe(services);
        expect(testItem!.behaviors).not.toBeNull();

        expect(testItem!.publicify_valueHosts.size).toBe(1);

        expect(testItem!.publicify_valueHostConfigs.size).toBe(1);
        expect(testItem!.publicify_InstanceState).not.toBeNull();
        expect(testItem!.publicify_InstanceState.stateChangeCounter).toBe(0);
        expect(testItem!.onValueChanged).toBeNull();
        expect(testItem!.onTextValueChanged).toBeNull();
        expect(testItem!.onConfigChanged).toBeNull();


        // ensure ValueHost is supporting the Config
        expect(testItem!.publicify_valueHosts.get('Field1')).toBeInstanceOf(StaticValueHost);

        // ensure the stored Config is the same as the one supplied
        expect(testItem!.publicify_valueHostConfigs.get('Field1')).not.toBe(configs[0]);
        expect(testItem!.publicify_valueHostConfigs.get('Field1')).toEqual(configs[0]);
    });

    test('Configs for 2 ValueHosts supplied. Other parameters are null', () => {
        let configs: Array<ValueHostConfig> = [
            {
                name: 'Field1',
                valueHostType: ValueHostType.Static,
                label: 'Field 1'
            },
            <StaticValueHostConfig>{
                valueHostType: ValueHostType.Static,
                name: 'Field2',
                label: 'Field 2',
                validatorConfigs: []
            }
        ];
        let testItem: Publicify_ValueHostsManager | null = null;
        let services = new MockJivsServices(false, false);
        expect(() => testItem = new Publicify_ValueHostsManager({ services: services, valueHostConfigs: configs })).not.toThrow();
        expect(testItem!.services).toBe(services);
        expect(testItem!.behaviors).not.toBeNull();

        expect(testItem!.publicify_valueHosts.size).toBe(2);

        expect(testItem!.publicify_valueHostConfigs.size).toBe(2);


        // ensure ValueHost is supporting the Config
        expect(testItem!.publicify_valueHosts.get('Field1')).toBeInstanceOf(StaticValueHost);
        expect(testItem!.publicify_valueHosts.get('Field2')).toBeInstanceOf(StaticValueHost);

        // ensure the stored Config is a copy of the one supplied
        expect(testItem!.publicify_valueHostConfigs.get('Field1')).not.toBe(configs[0]);
        expect(testItem!.publicify_valueHostConfigs.get('Field1')).toEqual(configs[0]);

        // when using the resolver, we don't have the original config.
        expect(testItem!.publicify_valueHostConfigs.get('Field2')).toEqual({
            name: 'Field2',
            label: 'Field 2',
            valueHostType: ValueHostType.Static,
            validatorConfigs: []
        });
    });

    test('Empty State object. Other parameters are null', () => {
        let state: string = createCapturedStateAsString([]);
        let testItem: Publicify_ValueHostsManager | null = null;
        let services = new MockJivsServices(false, false);
        expect(() => testItem = new Publicify_ValueHostsManager(
            { services: services, valueHostConfigs: [], capturedState: state })).not.toThrow();

        expect(testItem!.publicify_valueHosts.size).toBe(0);

        expect(testItem!.publicify_valueHostConfigs.size).toBe(0);
        expect(testItem!.publicify_InstanceState).not.toBeNull();
        expect(testItem!.publicify_InstanceState.stateChangeCounter).toBe(0);

        expect(testItem!.onValueChanged).toBeNull();
        expect(testItem!.onTextValueChanged).toBeNull();
        expect(testItem!.onConfigChanged).toBeNull();
        expect(testItem!.onValueHostValidationStateChanged).toBeNull();
        expect(testItem!.onValidationStateChanged).toBeNull();
    });
    test('Config and ValueHostInstanceState for 1 ValueHost supplied. Other parameters are null', () => {
        let configs: Array<ValueHostConfig> = [{
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        }];

        let savedValueHostInstanceStates: Array<ValueHostInstanceState> = [];
        savedValueHostInstanceStates.push({
            name: 'Field1',
            value: 10   // something we can return
        });
        let savedState = createCapturedStateAsString(savedValueHostInstanceStates);
        let testItem: Publicify_ValueHostsManager | null = null;
        let services = new MockJivsServices(false, false);
        expect(() => testItem = new Publicify_ValueHostsManager({
            services: services, valueHostConfigs: configs,
            capturedState: savedState
        })).not.toThrow();
        expect(testItem!.services).toBe(services);
        expect(testItem!.behaviors).not.toBeNull();

        expect(testItem!.publicify_valueHosts.size).toBe(1);

        expect(testItem!.publicify_valueHostConfigs.size).toBe(1);
        expect(testItem!.publicify_InstanceState).not.toBeNull();
        expect(testItem!.publicify_InstanceState.stateChangeCounter).toBe(0);

        expect(testItem!.onValueChanged).toBeNull();
        expect(testItem!.onTextValueChanged).toBeNull();
        expect(testItem!.onConfigChanged).toBeNull();


        // ensure ValueHost is supporting the Config and a Value of 10 from State
        expect(testItem!.publicify_valueHosts.get('Field1')).toBeInstanceOf(StaticValueHost);
        expect(testItem!.publicify_valueHosts.get('Field1')!.getValue()).toBe(10);

        // ensure the stored Config is the same as the one supplied
        expect(testItem!.publicify_valueHostConfigs.get('Field1')).not.toBe(configs[0]);
        expect(testItem!.publicify_valueHostConfigs.get('Field1')).toStrictEqual(configs[0]);
    });

    test('Callbacks supplied. Other parameters are null', () => {
        let setup: ValueHostsManagerConfig = {
            services: new MockJivsServices(false, false),
            valueHostConfigs: [],
            onValidationStateChanged: (valueHostsManager: IValueHostsManager, validationState: ValidationState) => { },
            onValueHostValidationStateChanged: (valueHost: IValidatableValueHostBase, snapshot: ValueHostValidationState) => { },
            onValueChanged: (valueHost: IValueHost, oldValue: any) => { },
            onTextValueChanged: (valueHost: IValidatableValueHostBase, oldValue: any) => { },
            onConfigChanged: (manager: IValueHostsManager, valueHostConfigs: Array<ValueHostConfig>) => { }
        };

        let testItem: Publicify_ValueHostsManager | null = null;
        expect(() => testItem = new Publicify_ValueHostsManager(setup)).not.toThrow();

        // other tests will confirm that the function correctly runs
        expect(testItem!.onValidationStateChanged).not.toBeNull();
        expect(testItem!.onValueHostValidationStateChanged).not.toBeNull();
        expect(testItem!.onValueChanged).not.toBeNull();
        expect(testItem!.onTextValueChanged).not.toBeNull();
        expect(testItem!.onConfigChanged).not.toBeNull();
    });

    test('Configure with an actual RegExp object to ensure that object is still present when needed', () => {
        let services = createJivsServicesForTesting({
            registerFormatters: true
        });
        let cf = services.conditionFactory as ConditionFactory;
        cf.register<RegExpConditionConfig>(ConditionType.RegExp, (config) => new RegExpCondition(config));
        let vhm = new ValueHostsManager({
            services: services,
            valueHostConfigs: [<FieldValueHostConfig>{
                valueHostType: ValueHostType.Field,
                name: 'Property1',
                dataType: LookupKey.String,
                validatorConfigs: [
                    {
                        conditionConfig: <RegExpConditionConfig>{
                            conditionType: ConditionType.RegExp,
                            valueHostName: 'Property1',
                            expression: /^ABC$/im // <<< monitoring this value
                        }

                    }
                ]
            }]
        });

        let vh = vhm.getFieldValueHost('Property1')!;
        vh.setValue('ABC');
        vh.validate();
        expect(vh.validationStatus).toBe(ValidationStatus.Valid);
        vh.setValue('ABCDEF');
        vh.validate();
        expect(vh.validationStatus).toBe(ValidationStatus.Invalid);
    });
    test('Configure with an actual Date object to ensure that object is still present when needed', () => {
        let services = createJivsServicesForTesting();
        let cf = services.conditionFactory as ConditionFactory;
        cf.register<EqualToValueConditionConfig>(ConditionType.EqualToValue, (config) => new EqualToValueCondition(config));
        let savedState = createCapturedStateAsString([
            {
                name: 'Property1',
                value: new Date(2000, 0, 1)
            }
        ]);
        let vhm = new ValueHostsManager({
            services: services,
            valueHostConfigs: [<FieldValueHostConfig>{
                valueHostType: ValueHostType.Field,
                name: 'Property1',
                dataType: LookupKey.Date,
                validatorConfigs: [
                    {
                        conditionConfig: <EqualToValueConditionConfig>{
                            conditionType: ConditionType.EqualToValue,
                            valueHostName: 'Property1',
                            secondValue: new Date(2000, 0, 1)
                        }

                    }
                ]
            }],
            capturedState: savedState
        });

        let vh = vhm.getFieldValueHost('Property1')!;
        vh.validate();
        expect(vh.validationStatus).toBe(ValidationStatus.Undetermined);
    });
});

function setupValueHostsManager(configs?: Array<FieldValueHostConfig> | null,
    savedVHMState?: ValueHostsManagerInstanceState | null,
    callbacks?: IValueHostsManagerCallbacks): {
        services: IJivsServices,
        valueHostsManager: Publicify_ValueHostsManager
    } {
    let services = createJivsServicesForTesting();
    services.autoGenerateDataTypeCheckService.enabled = false;
    services.dataTypeParserService.enabled = false;
    let savedState: string | undefined = undefined;
    if (savedVHMState)
    {
        let stateContainer = createStateContainer([]);
        stateContainer.vhm = savedVHMState;
        savedState = JSON.stringify(stateContainer);
    }

    let setup: ValueHostsManagerConfig = {
        services: services,
        valueHostConfigs: configs!,
        capturedState: savedState
    };
    if (callbacks)
        setup = { ...callbacks, ...setup } as ValueHostsManagerConfig;
    let vhm = new Publicify_ValueHostsManager(setup);

    return {
        services: services,
        valueHostsManager: vhm
    };
}

function setupFieldValueHostConfig(fieldIndex: number,
    conditionTypes: Array<string> | null): FieldValueHostConfig {
    let labelNumber = fieldIndex + 1;
    let config: FieldValueHostConfig = {
        name: `Field${labelNumber}`,
        valueHostType: ValueHostType.Field,
        label: `Field ${labelNumber}`,
        validatorConfigs: null,
    };
    if (conditionTypes)
        for (let conditionType of conditionTypes) {
            if (!config.validatorConfigs)
                config.validatorConfigs = [];
            config.validatorConfigs.push({
                conditionConfig: {
                    conditionType: conditionType
                },
                errorMessage: `Error ${labelNumber}: ${conditionType}`,
                summaryMessage: `Summary ${labelNumber}: ${conditionType}`
            });
        }

    return config;
}

function testValueHostInstanceState(testItem: Publicify_ValueHostsManager, valueHostName: ValueHostName,
    instanceState: Partial<FieldValueHostInstanceState> | null): void {
    let valueHost = testItem.publicify_valueHosts.get(valueHostName) as FieldValueHost;
    expect(valueHost).toBeDefined();
    expect(valueHost).toBeInstanceOf(FieldValueHost);

    if (!instanceState)
        instanceState = {};
    // fill in missing properties from factory createInstanceState defaults
    let factory = new ValueHostFactory();
    factory.register(new FieldValueHostGenerator());
    let config = testItem.publicify_valueHostConfigs.get(valueHostName) as FieldValueHostConfig;
    let defaultState = factory.createInstanceState(config) as FieldValueHostInstanceState;

    let stateToCompare: FieldValueHostInstanceState = { ...defaultState, ...instanceState, };

    // ensure ValueHost has an initial state. Use updateState() because it is the only time we can see the real state
    valueHost.updateInstanceState((stateToUpdate) => {
        expect(stateToUpdate).toEqual(stateToCompare);
        return stateToUpdate;
    }, valueHost);
}
function testStaticValueHostInstanceState(testItem: Publicify_ValueHostsManager, valueHostName: ValueHostName,
    instanceState: Partial<StaticValueHostInstanceState> | null): void {
    let valueHost = testItem.publicify_valueHosts.get(valueHostName) as StaticValueHost;
    expect(valueHost).toBeDefined();
    expect(valueHost).toBeInstanceOf(StaticValueHost);

    if (!instanceState)
        instanceState = {};
    // fill in missing properties from factory createInstanceState defaults
    let factory = new ValueHostFactory();
    factory.register(new StaticValueHostGenerator());
    let config = testItem.publicify_valueHostConfigs.get(valueHostName) as StaticValueHostConfig;
    let defaultState = factory.createInstanceState(config) as StaticValueHostInstanceState;

    let stateToCompare: StaticValueHostInstanceState = { ...defaultState, ...instanceState, };

    // ensure ValueHost has an initial state. Use updateState() because it is the only time we can see the real state
    valueHost.updateInstanceState((stateToUpdate) => {
        expect(stateToUpdate).toEqual(stateToCompare);
        return stateToUpdate;
    }, valueHost);
}

describe('ValueHostsManager.addValueHost', () => {

    test('New ValueHostConfig with no previous state creates ValueHost, adds Config, and creates state', () => {
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false),
            valueHostConfigs: []
        });
        let config: ValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        expect(() => testItem.addValueHost(config, null)).not.toThrow();

        expect(testItem.publicify_valueHosts).not.toBeNull();
        expect(testItem.publicify_valueHosts.size).toBe(1);
        expect(testItem.publicify_valueHostConfigs.size).toBe(1);
        expect(testItem.publicify_InstanceState).not.toBeNull();
        expect(testItem.publicify_InstanceState.stateChangeCounter).toBe(0);

        // ensure the stored Config is the same as the one supplied
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config);
        expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config);

        // Check the valueHosts type and initial state
        testStaticValueHostInstanceState(testItem, 'Field1', null);
    });
    test('Second ValueHost with same name throws', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config1: ValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        expect(() => testItem.addValueHost(config1, null)).not.toThrow();
        let config2: ValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        expect(() => testItem.addValueHost(config1, null)).toThrow();
    });
    test('Add2 Configs. ValueHosts and states are generated for both.', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config1: StaticValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        let initialValueHost1 = testItem.addValueHost(config1, null);
        let config2: StaticValueHostConfig = {
            name: 'Field2',
            valueHostType: ValueHostType.Static,
            label: 'Field 2'
        };
        let initialValueHost2 = testItem.addValueHost(config2, null);

        expect(testItem.publicify_valueHosts).not.toBeNull();
        expect(testItem.publicify_valueHosts.size).toBe(2);
        expect(testItem.publicify_valueHosts.get('Field1')).toBe(initialValueHost1);
        expect(testItem.publicify_valueHosts.get('Field2')).toBe(initialValueHost2);
        expect(testItem.publicify_valueHostConfigs).not.toBeNull();
        expect(testItem.publicify_valueHostConfigs.size).toBe(2);
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config1);
        expect(testItem.publicify_valueHostConfigs.get('Field2')).toEqual(config2);
        expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config1);
        expect(testItem.publicify_valueHostConfigs.get('Field2')).not.toBe(config2);
        expect(testItem.publicify_InstanceState).not.toBeNull();
        expect(testItem.publicify_InstanceState.stateChangeCounter).toBe(0);

        // Check the valueHosts type and initial state
        testStaticValueHostInstanceState(testItem, 'Field1', null);
        // Check the valueHosts type and initial state
        testStaticValueHostInstanceState(testItem, 'Field2', null);
    });
    test('New ValueHostConfig with provided state creates ValueHost, adds Config, and uses the provided state', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config: ValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        let state: ValueHostInstanceState = {
            name: 'Field1',
            value: 'ABC'
        };
        expect(() => testItem.addValueHost(config, state)).not.toThrow();

        expect(testItem.publicify_valueHosts).not.toBeNull();
        expect(testItem.publicify_valueHosts.size).toBe(1);
        expect(testItem.publicify_valueHostConfigs).not.toBeNull();
        expect(testItem.publicify_valueHostConfigs.size).toBe(1);
        expect(testItem.publicify_InstanceState).not.toBeNull();
        expect(testItem.publicify_InstanceState.stateChangeCounter).toBe(0);

        // ensure the stored Config is the same as the one supplied
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config);
        expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config);

        // Check the valueHosts type and initial state
        testStaticValueHostInstanceState(testItem, 'Field1', {
            value: 'ABC'
        });
    });
});
describe('addToValueHosts with FieldValueHosts', () => {

    test('Add FieldValueHostConfig with required ConditionConfig', () => {
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(true, false), valueHostConfigs: []
        });
        let config: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: [
                {
                    conditionConfig: {
                        conditionType: ConditionType.RequireText,
                    },
                    errorMessage: 'msg'
                }
            ]
        };
        testItem.addValueHost(config, null);
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toBeDefined();
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config);
    });

    test('InstanceState with ValidationStatus=Valid already exists for the ValueHostConfig being added. That state is used', () => {

        let savedValueHostInstanceState: FieldValueHostInstanceState = {
            name: 'Field1',
            status: ValidationStatus.Valid, // something we can return
            value: 10,   // something we can return,
            issuesFound: null
        };
        let savedState = createCapturedStateAsString([savedValueHostInstanceState]);
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false), valueHostConfigs: [],
            capturedState: savedState
        });
        let config: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: [
                {
                    conditionConfig: {
                        conditionType: ConditionType.RequireText,
                    },
                    errorMessage: 'msg'
                }
            ]
        };
        testItem.addValueHost(config, null);

        testValueHostInstanceState(testItem, 'Field1', savedValueHostInstanceState);
    });

    test('InstanceState with ValidationStatus=Invalid already exists for the ValueHostConfig being added.', () => {

        let savedValueHostInstanceState: FieldValueHostInstanceState = {
            name: 'Field1',
            status: ValidationStatus.Invalid, // something we can return
            value: 10,   // something we can return,
            issuesFound: [{
                errorMessage: 'msg',
                valueHostName: 'Field1',
                errorCode: ConditionType.RequireText,
                severity: ValidationSeverity.Error
            }]
        };
        let savedState = createCapturedStateAsString([savedValueHostInstanceState]);
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false), valueHostConfigs: [],
            capturedState: savedState
        });
        let config: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: [
                {
                    conditionConfig: {
                        conditionType: ConditionType.RequireText,
                    },
                    errorMessage: 'msg'
                }
            ]
        };
        testItem.addValueHost(config, null);

        testValueHostInstanceState(testItem, 'Field1', savedValueHostInstanceState);
    });

    test('InstanceState already exists in two places: lastValueHostInstanceState and as parameter for addValueHost. State is sourced from addValueHost.', () => {
        let config: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: [
                {
                    conditionConfig: {
                        conditionType: ConditionType.RequireText,
                    },
                    errorMessage: 'msg'
                }
            ]
        };

        let savedValueHostInstanceStates: Array<ValueHostInstanceState> = [];
        savedValueHostInstanceStates.push(<FieldValueHostInstanceState>{
            name: 'Field1',
            status: ValidationStatus.Valid, // something we can return
            value: 10   // something we can return
        });
        let savedState = createCapturedStateAsString(savedValueHostInstanceStates);
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false), valueHostConfigs: [],
            capturedState: savedState
        });
        let addState: FieldValueHostInstanceState = {
            name: 'Field1',
            value: 20,
            status: ValidationStatus.Invalid,
            issuesFound: [{
                errorMessage: 'msg',
                valueHostName: 'Field1',
                errorCode: ConditionType.RequireText,
                severity: ValidationSeverity.Error
            }]
        };
        testItem.addValueHost(config, addState);

        testValueHostInstanceState(testItem, 'Field1', addState);
    });

    test('InstanceState instance is changed after passing in has no impact on stored state', () => {

        let savedValueHostInstanceState: FieldValueHostInstanceState = {
            name: 'Field1',
            status: ValidationStatus.Valid, // something we can return
            value: 10,   // something we can return,
            issuesFound: null
        };
        let savedValueHostInstanceStates: Array<ValueHostInstanceState> = [savedValueHostInstanceState];
        let savedState = createCapturedStateAsString(savedValueHostInstanceStates);
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false), valueHostConfigs: [],
            capturedState: savedState
        });
        let config: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: [
                {
                    conditionConfig: {
                        conditionType: ConditionType.RequireText,
                    },
                    errorMessage: 'msg'
                }
            ]
        };
        testItem.addValueHost(config, null);
        let copiedLastState = deepClone(savedValueHostInstanceState) as FieldValueHostInstanceState;
        savedValueHostInstanceState.value = 20;

        testValueHostInstanceState(testItem, 'Field1', copiedLastState);
    });
});
describe('addOrUpdateValueHost completely replaces the ValueHost instance', () => {
    describe('Using FieldValueHostConfig', () => {
        test('Replace the config to install a validator', () => {
            let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
            let config: FieldValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Field,
                label: 'Field 1',
                validatorConfigs: null,
            };
            let initialValueHost = testItem.addValueHost(config, null);

            let replacementConfig = { ...config };
            replacementConfig.validatorConfigs = [
                {
                    conditionConfig: {
                        conditionType: AlwaysMatchesConditionType,
                    },
                    errorMessage: 'Error'
                }
            ];
            let replacementValidatorConfig = replacementConfig.validatorConfigs[0];

            let replacementValueHost: IValueHost | null = null;
            expect(() => replacementValueHost = testItem.addOrUpdateValueHost(replacementConfig, null)).not.toThrow();
            expect(replacementValueHost).not.toBeNull();
            expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced

            expect(testItem.publicify_valueHosts.size).toBe(1);
            expect(testItem.publicify_valueHosts.get('Field1')).toBe(replacementValueHost);
            expect(testItem!.publicify_valueHostConfigs.size).toBe(1);
            expect(testItem.publicify_InstanceState).not.toBeNull();

            // no side effects of the originals
            expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toEqual(config);
            expect(config.validatorConfigs).toBeNull();

            // ensure the stored Config is the same as the one supplied
            expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(replacementConfig);
            expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(replacementConfig);
            expect(replacementConfig.validatorConfigs[0]).toBe(replacementValidatorConfig);  // no side effects

            // ensure ValueHost is FieldValueHost and has an initial state
            testValueHostInstanceState(testItem, 'Field1', null);
        });
        test('addOrUpdateValueHost works like addValueHost with unknown ValueHostConfig', () => {
            let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
            let config: ValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Field,
                label: 'Field 1'
            };
            expect(() => testItem.addOrUpdateValueHost(config, null)).not.toThrow();


            expect(testItem.publicify_valueHosts).not.toBeNull();
            expect(testItem.publicify_valueHosts.size).toBe(1);
            expect(testItem.publicify_valueHostConfigs).not.toBeNull();
            expect(testItem.publicify_valueHostConfigs.size).toBe(1);
            expect(testItem.publicify_InstanceState).not.toBeNull();

            // ensure the stored Config is the same as the one supplied
            expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config);
            expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config);

            // ensure ValueHost is FieldValueHost and has an initial state
            testValueHostInstanceState(testItem, 'Field1', null);
        });

        test('Replace the config with existing ValueHostInstanceState.ValidationStatus of Invalid retains state when replacement is the same type', () => {
            let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
            let config: FieldValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Field,
                label: 'Field 1',
                validatorConfigs: null,
            };
            let initialValueHost = testItem.addValueHost(config, null);

            let replacementConfig = { ...config };
            replacementConfig.validatorConfigs = [
                {
                    conditionConfig: {
                        conditionType: AlwaysMatchesConditionType
                    },
                    errorMessage: 'Error'
                }
            ];
            let replacementValidatorConfig = replacementConfig.validatorConfigs[0];

            let replacementValueHost: IValueHost | null = null;
            expect(() => replacementValueHost = testItem.addOrUpdateValueHost(replacementConfig, null)).not.toThrow();
            expect(replacementValueHost).not.toBeNull();
            expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced


            expect(testItem.publicify_valueHosts.size).toBe(1);
            expect(testItem.publicify_valueHosts.get('Field1')).toBe(replacementValueHost);

            expect(testItem!.publicify_valueHostConfigs.size).toBe(1);
            expect(testItem.publicify_InstanceState).not.toBeNull();

            // no side effects of the originals
            expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config);
            expect(config.validatorConfigs).toBeNull();

            // ensure ValueHost is supporting the Config
            expect(testItem.publicify_valueHosts.get('Field1')).toBeInstanceOf(FieldValueHost);

            // ensure the stored Config is the same as the one supplied
            expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(replacementConfig);
            expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(replacementConfig);
            expect(replacementConfig.validatorConfigs[0]).toBe(replacementValidatorConfig);  // no side effects

            // ensure ValueHost is FieldValueHost and has an initial state
            testValueHostInstanceState(testItem, 'Field1', null);
        });

        test('Replace the state, keeping the same config. Confirm the state and config', () => {
            let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
            let config: FieldValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Field,
                label: 'Field 1',
                validatorConfigs: [
                    {
                        conditionConfig: {
                            conditionType: AlwaysMatchesConditionType,
                        },
                        errorMessage: 'Error'
                    }
                ]
            };
            let initialValueHost = testItem.addValueHost(config, null);

            let updateState: FieldValueHostInstanceState = {
                name: 'Field1',
                value: 40,
                issuesFound: null,
                status: ValidationStatus.NotAttempted
            };
            let replacementValueHost: IValueHost | null = null;
            expect(() => replacementValueHost = testItem.addOrUpdateValueHost(config, updateState)).not.toThrow();
            expect(replacementValueHost).not.toBeNull();
            expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced

            // ensure the stored Config is the same as the one supplied
            expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config);
            expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config);

            // ensure ValueHost is FieldValueHost and has an initial state
            testValueHostInstanceState(testItem, 'Field1', updateState);
        });
        test('Edit state instance after addOrUpdateValueHost has no impact on state in ValueHost', () => {
            let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
            let config: FieldValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Field,
                label: 'Field 1',
                validatorConfigs: [
                    {
                        conditionConfig: {
                            conditionType: AlwaysMatchesConditionType,
                        },
                        errorMessage: 'Error'
                    }
                ]
            };

            let initialValueHost = testItem.addValueHost(config, null);

            let updateState: FieldValueHostInstanceState = {
                name: 'Field1',
                value: 40,
                issuesFound: null,
                status: ValidationStatus.NotAttempted
            };
            testItem.addOrUpdateValueHost(config, updateState);

            let savedState = deepClone(updateState);
            updateState.value = 100;

            // ensure ValueHost is FieldValueHost and has an initial state
            testValueHostInstanceState(testItem, 'Field1', savedState);
        });
    });
    describe('Using StaticValueHostConfig', () => {
        test('Replace the config to install a validator', () => {
            let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
            let config: StaticValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Static,
                label: 'Field 1',
            };
            let initialValueHost = testItem.addValueHost(config, null);

            let replacementConfig = { ...config };
            replacementConfig.dataType = 'ALT';
            replacementConfig.label = 'ALT Label';

            let replacementValueHost: IValueHost | null = null;
            expect(() => replacementValueHost = testItem.addOrUpdateValueHost(replacementConfig, null)).not.toThrow();
            expect(replacementValueHost).not.toBeNull();
            expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced

            expect(testItem.publicify_valueHosts).not.toBeNull();
            expect(testItem.publicify_valueHosts.size).toBe(1);
            expect(testItem.publicify_valueHosts.get('Field1')).toBe(replacementValueHost);
            expect(testItem.publicify_valueHostConfigs).not.toBeNull();
            expect(testItem.publicify_valueHostConfigs.size).toBe(1);
            expect(testItem.publicify_InstanceState).not.toBeNull();

            // no side effects of the originals
            expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toEqual(config);
            expect(config.label).toBe('Field 1');

            // ensure the stored Config is the same as the one supplied
            expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(replacementConfig);

            // ensure ValueHost is StaticValueHost and has an initial state
            testStaticValueHostInstanceState(testItem, 'Field1', null);
        });
        test('addOrUpdateValueHost works like addValueHost with unknown ValueHostConfig', () => {
            let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
            let config: ValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Static,
                label: 'Field 1'
            };
            expect(() => testItem.addOrUpdateValueHost(config, null)).not.toThrow();

            expect(testItem.publicify_valueHosts).not.toBeNull();
            expect(testItem.publicify_valueHosts.size).toBe(1);
            expect(testItem.publicify_valueHostConfigs).not.toBeNull();
            expect(testItem.publicify_valueHostConfigs.size).toBe(1);
            expect(testItem.publicify_InstanceState).not.toBeNull();

            // ensure the stored Config is the same as the one supplied
            expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config);
            expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config);

            // ensure ValueHost is StaticValueHost and has an initial state
            testStaticValueHostInstanceState(testItem, 'Field1', null);
        });

        test('Replace the state, keeping the same config. Confirm the state and config', () => {
            let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
            let config: StaticValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Static,
                label: 'Field 1'
            };
            let initialValueHost = testItem.addValueHost(config, null);

            let updateState: StaticValueHostInstanceState = {
                name: 'Field1',
                value: 40
            };
            let replacementValueHost: IValueHost | null = null;
            expect(() => replacementValueHost = testItem.addOrUpdateValueHost(config, updateState)).not.toThrow();
            expect(replacementValueHost).not.toBeNull();
            expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced

            // ensure the stored Config is the same as the one supplied
            expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config);
            expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config);

            // ensure ValueHost is StaticValueHost and has an initial state
            testStaticValueHostInstanceState(testItem, 'Field1', updateState);
        });
        test('Edit state instance after addOrUpdateValueHost has no impact on state in ValueHost', () => {
            let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
            let config: StaticValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Static,
                label: 'Field 1',
            };
            let initialValueHost = testItem.addValueHost(config, null);

            let updateState: StaticValueHostInstanceState = {
                name: 'Field1',
                value: 40
            };
            testItem.addOrUpdateValueHost(config, updateState);

            let savedState = deepClone(updateState);
            updateState.value = 100;

            // ensure ValueHost is StaticValueHost and has an initial state
            testStaticValueHostInstanceState(testItem, 'Field1', savedState);
        });
        test('Confirm previous ValueHost is discarded and new one retains the state from the previous one', () => {
            let config: StaticValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Static,
                label: 'Field 1'
            };
            let testItem = new Publicify_ValueHostsManager({
                services: new MockJivsServices(false, false),
                valueHostConfigs: [config]
            });

            let initialValueHost = testItem.getValueHost('Field1')!
            initialValueHost.setValue(100); // some stateful info
            let savedValue = initialValueHost.getValue();

            let updatedConfig: StaticValueHostConfig = { ...config, label: 'Label changed' };

            let replacementValueHost = testItem.addOrUpdateValueHost(updatedConfig, null);
            expect(replacementValueHost).not.toBeNull();
            expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced
            expect(replacementValueHost.getValue()).toBe(savedValue);
            expect(replacementValueHost.getLabel()).toBe('Label changed');

            expect(() => initialValueHost.getFromInstanceState('anything')).toThrow();  // deref error
        });
        test('Confirm previous ValueHost is discarded and but uses the state passed into addOrUpdateValueHost', () => {
            let config: StaticValueHostConfig = {
                name: 'Field1',
                valueHostType: ValueHostType.Static,
                label: 'Field 1'
            };
            let testItem = new Publicify_ValueHostsManager({
                services: new MockJivsServices(false, false),
                valueHostConfigs: [config]
            });

            let initialValueHost = testItem.getValueHost('Field1')!
            initialValueHost.setValue(100); // some stateful info
            let savedValue = initialValueHost.getValue();

            let updatedConfig: StaticValueHostConfig = { ...config, label: 'Label changed' };
            let updateState: StaticValueHostInstanceState = {
                name: 'Field1',
                value: 40
            };
            let replacementValueHost = testItem.addOrUpdateValueHost(updatedConfig, updateState);
            expect(replacementValueHost).not.toBeNull();
            expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced
            expect(replacementValueHost.getValue()).toBe(updateState.value);    // different state
            expect(replacementValueHost.getLabel()).toBe('Label changed');

            expect(() => initialValueHost.getFromInstanceState('anything')).toThrow();  // deref error
        });
    });
});
describe('ValueHostsManager.discardValueHost completely removes ValueHost, its state and config', () => {
    test('After adding in the VM Config, discard the only one leaves empty valueHosts, configs, and state', () => {
        let config: StaticValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        let savedState = createCapturedStateAsString([{
            name: config.name,
            value: 10
        }]);
        let setup: ValueHostsManagerConfig = {
            services: new MockJivsServices(false, false),
            valueHostConfigs: [config],
            capturedState: savedState
        };
        let testItem = new Publicify_ValueHostsManager(setup);
        let initialValueHost = testItem.getValueHost(config.name)!;
        expect(initialValueHost.getValue()).toBe(10);  // to prove later this is deleted

        expect(() => testItem.discardValueHost(config.name)).not.toThrow();

        expect(testItem.publicify_valueHosts).not.toBeNull();
        expect(testItem.publicify_valueHosts.size).toBe(0);
        expect(testItem.publicify_valueHostConfigs).not.toBeNull();
        expect(testItem.publicify_valueHostConfigs.size).toBe(0);
        expect(testItem.publicify_InstanceState).not.toBeNull();

        expect(() => initialValueHost.getFromInstanceState('anything')).toThrow();

        // add back the config to confirm the original state (value=10) was discarded
        let addedVH = testItem.addValueHost(config, null);
        expect(addedVH.getValue()).toBeUndefined();

    });
    test('Discard the only one leaves empty valueHosts, configs, and state', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config: StaticValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        let initialValueHost = testItem.addValueHost(config, null);

        expect(() => testItem.discardValueHost(config.name)).not.toThrow();

        expect(testItem.publicify_valueHosts).not.toBeNull();
        expect(testItem.publicify_valueHosts.size).toBe(0);
        expect(testItem.publicify_valueHostConfigs).not.toBeNull();
        expect(testItem.publicify_valueHostConfigs.size).toBe(0);
        expect(testItem.publicify_InstanceState).not.toBeNull();

        expect(() => initialValueHost.getFromInstanceState('anything')).toThrow();

    });

    test('Start with 2 Configs and discard one retains only the expected ValueHost, its state and config', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config1: StaticValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        let initialValueHost1 = testItem.addValueHost(config1, null);
        let config2: StaticValueHostConfig = {
            name: 'Field2',
            valueHostType: ValueHostType.Static,
            label: 'Field 2'
        };
        let initialValueHost2 = testItem.addValueHost(config2, null);

        expect(() => testItem.discardValueHost(config2.name)).not.toThrow();

        expect(testItem.publicify_valueHosts).not.toBeNull();
        expect(testItem.publicify_valueHosts.size).toBe(1);
        expect(testItem.publicify_valueHosts.get('Field1')).toBe(initialValueHost1);
        expect(testItem.publicify_valueHostConfigs).not.toBeNull();
        expect(testItem.publicify_valueHostConfigs.size).toBe(1);
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config1);
        expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config1);
        expect(testItem.publicify_InstanceState).not.toBeNull();

    });
});
describe('ValueHostsManager.addOrMergeValueHost', () => {
    test('Replace the config to install a validator', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: null,
        };
        let initialValueHost = testItem.addValueHost(config, null);

        let replacementConfig = { ...config };
        replacementConfig.validatorConfigs = [
            {
                conditionConfig: {
                    conditionType: AlwaysMatchesConditionType,
                },
                errorMessage: 'Error'
            }
        ];
        const expectedConfig: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: [
                {
                    conditionConfig: {
                        conditionType: AlwaysMatchesConditionType
                    },
                    errorMessage: 'Error'
                }
            ]
        };
        let replacementValidatorConfig = replacementConfig.validatorConfigs[0];

        let replacementValueHost: IValueHost | null = null;
        expect(() => replacementValueHost = testItem.addOrMergeValueHost(replacementConfig, null)).not.toThrow();
        expect(replacementValueHost).not.toBeNull();
        expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced


        expect(testItem.publicify_valueHosts.size).toBe(1);
        expect(testItem.publicify_valueHosts.get('Field1')).toBe(replacementValueHost);
        expect(testItem!.publicify_valueHostConfigs.size).toBe(1);
        expect(testItem.publicify_InstanceState).not.toBeNull();

        // no side effects of the originals
        expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config);
        expect(config.validatorConfigs).toBeNull();

        // ensure the stored Config has the merged data
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(expectedConfig);

        // ensure ValueHost is FieldValueHost and has an initial state
        testValueHostInstanceState(testItem, 'Field1', null);
    });
    test('addOrMergeValueHost works like addValueHost with unknown ValueHostConfig', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config: ValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1'
        };
        expect(() => testItem.addOrMergeValueHost(config, null)).not.toThrow();


        expect(testItem.publicify_valueHosts.size).toBe(1);
        expect(testItem!.publicify_valueHostConfigs.size).toBe(1);
        expect(testItem.publicify_InstanceState).not.toBeNull();

        // ensure the stored Config is the same as the one supplied
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config);

        // ensure ValueHost is FieldValueHost and has an initial state
        testValueHostInstanceState(testItem, 'Field1', null);
    });

    test('Replace the config with existing ValueHostInstanceState.ValidationStatus of Invalid retains state when replacement is the same type', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: null,
        };
        let initialValueHost = testItem.addValueHost(config, null);

        let replacementConfig = { ...config };
        replacementConfig.validatorConfigs = [
            {
                conditionConfig: {
                    conditionType: AlwaysMatchesConditionType
                },
                errorMessage: 'Error'
            }
        ];

        const expectedConfig: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: [
                {
                    conditionConfig: {
                        conditionType: AlwaysMatchesConditionType
                    },
                    errorMessage: 'Error'
                }
            ]
        };

        let replacementValueHost: IValueHost | null = null;
        expect(() => replacementValueHost = testItem.addOrMergeValueHost(replacementConfig, null)).not.toThrow();
        expect(replacementValueHost).not.toBeNull();
        expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced


        expect(testItem.publicify_valueHosts.size).toBe(1);
        expect(testItem.publicify_valueHosts.get('Field1')).toBe(replacementValueHost);
        expect(testItem!.publicify_valueHostConfigs.size).toBe(1);
        expect(testItem.publicify_InstanceState).not.toBeNull();

        // no side effects of the originals
        expect(testItem.publicify_valueHostConfigs.get('Field1')).not.toBe(config);
        expect(config.validatorConfigs).toBeNull();

        // ensure ValueHost is supporting the Config
        expect(testItem.publicify_valueHosts.get('Field1')).toBeInstanceOf(FieldValueHost);

        // ensure the stored Config is the expected data
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(expectedConfig);

        // ensure ValueHost is FieldValueHost and has an initial state
        testValueHostInstanceState(testItem, 'Field1', null);
    });
    test('Complex merge has correct results', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: [
                {   // this does not match in the replacement
                    errorCode: 'Q',
                    conditionConfig: { conditionType: ConditionType.NotNull }
                },
                {   // replacement changes its error message
                    errorCode: 'R',
                    errorMessage: 'Required',
                    conditionConfig: { conditionType: ConditionType.RequireText }
                }
            ]
        };
        let initialValueHost = testItem.addValueHost(config, null);

        let replacementConfig = { ...config, dataType: LookupKey.String, label: 'First name' };

        replacementConfig.validatorConfigs = [
            {
                errorCode: 'R',
                errorMessage: 'Changed',
                conditionConfig: { conditionType: ConditionType.RequireText }
            },
            {
                conditionConfig: {
                    conditionType: AlwaysMatchesConditionType
                },
                errorMessage: 'Always'
            },
        ];

        const expectedConfig: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            dataType: LookupKey.String,
            label: 'First name',
            validatorConfigs: [
                {
                    errorCode: 'Q',
                    conditionConfig: { conditionType: ConditionType.NotNull }
                },
                {
                    errorCode: 'R',
                    errorMessage: 'Changed',
                    conditionConfig: { conditionType: ConditionType.RequireText }
                },
                {
                    conditionConfig: {
                        conditionType: AlwaysMatchesConditionType
                    },
                    errorMessage: 'Always'
                },
            ]
        };

        let replacementValueHost: IValueHost | null = null;
        expect(() => replacementValueHost = testItem.addOrMergeValueHost(replacementConfig, null)).not.toThrow();
        expect(replacementValueHost).not.toBeNull();
        expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced

        // ensure ValueHost is supporting the Config
        expect(testItem.publicify_valueHosts.get('Field1')).toBeInstanceOf(FieldValueHost);

        // ensure the stored Config is the expected data
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(expectedConfig);

        // ensure ValueHost is FieldValueHost and has an initial state
        testValueHostInstanceState(testItem, 'Field1', null);
    });

    test('Replace the state, keeping the same config. Confirm the state and config', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: [
                {
                    conditionConfig: {
                        conditionType: AlwaysMatchesConditionType,
                    },
                    errorMessage: 'Error'
                }
            ]
        };
        let initialValueHost = testItem.addValueHost(config, null);

        let updateState: FieldValueHostInstanceState = {
            name: 'Field1',
            value: 40,
            issuesFound: null,
            status: ValidationStatus.NotAttempted
        };
        let replacementValueHost: IValueHost | null = null;
        expect(() => replacementValueHost = testItem.addOrMergeValueHost(config, updateState)).not.toThrow();
        expect(replacementValueHost).not.toBeNull();
        expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced

        // ensure the stored Config is the same as the one supplied
        expect(testItem.publicify_valueHostConfigs.get('Field1')).toEqual(config);

        // ensure ValueHost is FieldValueHost and has an initial state
        testValueHostInstanceState(testItem, 'Field1', updateState);
    });
    test('Edit state instance after addOrMergeValueHost has no impact on state in ValueHost', () => {
        let testItem = new Publicify_ValueHostsManager({ services: new MockJivsServices(false, false), valueHostConfigs: [] });
        let config: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: [
                {
                    conditionConfig: {
                        conditionType: AlwaysMatchesConditionType,
                    },
                    errorMessage: 'Error'
                }
            ]
        };
        let initialValueHost = testItem.addValueHost(config, null);

        let updateState: FieldValueHostInstanceState = {
            name: 'Field1',
            value: 40,
            issuesFound: null,
            status: ValidationStatus.NotAttempted
        };
        testItem.addOrMergeValueHost(config, updateState);

        let savedState = deepClone(updateState);
        updateState.value = 100;

        // ensure ValueHost is FieldValueHost and has an initial state
        testValueHostInstanceState(testItem, 'Field1', savedState);
    });
    test('Confirm previous ValueHost is discarded and new one retains the state from the previous one', () => {
        let config: StaticValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false),
            valueHostConfigs: [config]
        });

        let initialValueHost = testItem.getValueHost('Field1')!
        initialValueHost.setValue(100); // some stateful info
        let savedValue = initialValueHost.getValue();

        let updatedConfig: StaticValueHostConfig = { ...config, label: 'Label changed' };
        let replacementValueHost = testItem.addOrMergeValueHost(updatedConfig, null);
        expect(replacementValueHost).not.toBeNull();
        expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced
        expect(replacementValueHost.getValue()).toBe(savedValue);
        expect(replacementValueHost.getLabel()).toBe('Label changed');
        expect(() => initialValueHost.getFromInstanceState('anything')).toThrow();  // deref error
    });
    test('Confirm previous ValueHost is discarded and but uses the state passed into addOrUpdateValueHost', () => {
        let config: StaticValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false),
            valueHostConfigs: [config]
        });

        let initialValueHost = testItem.getValueHost('Field1')!
        initialValueHost.setValue(100); // some stateful info
        let savedValue = initialValueHost.getValue();

        let updatedConfig: StaticValueHostConfig = { ...config, label: 'Label changed' };
        let updateState: StaticValueHostInstanceState = {
            name: 'Field1',
            value: 40
        };
        let replacementValueHost = testItem.addOrMergeValueHost(updatedConfig, updateState);
        expect(replacementValueHost).not.toBeNull();
        expect(replacementValueHost).not.toBe(initialValueHost);   // completely replaced
        expect(replacementValueHost.getValue()).toBe(updateState.value);    // different state
        expect(replacementValueHost.getLabel()).toBe('Label changed');

        expect(() => initialValueHost.getFromInstanceState('anything')).toThrow();  // deref error
    });
});
describe('getValueHost, getValidatorsValueHost, getFieldValueHost, getCalcValueHost, getStaticValueHost, getFieldByElementIdentifier, getFieldByPropertyName', () => {
    test('With 2 FieldValueHostConfigs, get each with all functions. Expect null for Calc and Static', () => {

        let config1: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: null,
        };
        let config2: FieldValueHostConfig = {
            name: 'Field2',
            valueHostType: ValueHostType.Field,
            label: 'Field 2',
            validatorConfigs: null,
        };
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false),
            valueHostConfigs: [config1, config2]
        });
        let vh1: IValueHost | null = null;
        expect(() => vh1 = testItem.getValueHost('Field1')).not.toThrow();
        expect(vh1).toBeInstanceOf(FieldValueHost);
        expect(vh1!.getName()).toBe('Field1');
        let vh2: IValueHost | null = null;
        expect(() => vh2 = testItem.getValueHost('Field2')).not.toThrow();
        expect(vh2).toBeInstanceOf(FieldValueHost);
        expect(vh2!.getName()).toBe('Field2');
        let vh3: IValidatorsValueHostBase | null = null;
        expect(() => vh3 = testItem.getValidatorsValueHost('Field1')).not.toThrow();
        expect(vh3).toBeInstanceOf(ValidatableValueHostBase);
        expect(vh3!.getName()).toBe('Field1');
        let vh4: IValidatorsValueHostBase | null = null;
        expect(() => vh4 = testItem.getValidatorsValueHost('Field2')).not.toThrow();
        expect(vh4).toBeInstanceOf(ValidatableValueHostBase);
        expect(vh4!.getName()).toBe('Field2');
        let vh5: IFieldValueHost | null = null;
        expect(() => vh5 = testItem.getFieldValueHost('Field1')).not.toThrow();
        expect(vh5).toBeInstanceOf(FieldValueHost);
        expect(vh5!.getName()).toBe('Field1');
        let vh6: IFieldValueHost | null = null;
        expect(() => vh6 = testItem.getFieldValueHost('Field2')).not.toThrow();
        expect(vh6).toBeInstanceOf(FieldValueHost);
        expect(vh6!.getName()).toBe('Field2');
        let vh9: ICalcValueHost | null = null;
        expect(() => vh9 = testItem.getCalcValueHost('Field2')).not.toThrow();
        expect(vh9).toBeNull();
        let vh10: IStaticValueHost | null = null;
        expect(() => vh10 = testItem.getStaticValueHost('Field2')).not.toThrow();
        expect(vh10).toBeNull();

    });
    test('With 2 Array<ValueHostConfig>, get each with both functions. getValueHost returns VH, getValidatorsValueHost and getFieldValueHost return null', () => {

        let config1: ValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Static,
            label: 'Field 1'
        };
        let config2: ValueHostConfig = {
            name: 'Field2',
            valueHostType: ValueHostType.Calc,
            label: 'Field 2'
        };
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false),
            valueHostConfigs: [config1, config2]
        });
        let vh1: IValueHost | null = null;
        expect(() => vh1 = testItem.getValueHost('Field1')).not.toThrow();
        expect(vh1).toBeInstanceOf(StaticValueHost);
        expect(vh1!.getName()).toBe('Field1');
        let vh2: IValueHost | null = null;
        expect(() => vh2 = testItem.getValueHost('Field2')).not.toThrow();
        expect(vh2).toBeInstanceOf(CalcValueHost);
        expect(vh2!.getName()).toBe('Field2');
        let vh3: IValidatorsValueHostBase | null = null;
        expect(() => vh3 = testItem.getValidatorsValueHost('Field1')).not.toThrow();
        expect(vh3).toBeNull();
        let vh4: IValidatorsValueHostBase | null = null;
        expect(() => vh4 = testItem.getValidatorsValueHost('Field2')).not.toThrow();
        expect(vh4).toBeNull();
        let vh5: IFieldValueHost | null = null;
        expect(() => vh5 = testItem.getFieldValueHost('Field1')).not.toThrow();
        expect(vh5).toBeNull();
        let vh6: IFieldValueHost | null = null;
        expect(() => vh6 = testItem.getFieldValueHost('Field2')).not.toThrow();
        expect(vh6).toBeNull();
        let vh9: ICalcValueHost | null = null;
        expect(() => vh9 = testItem.getCalcValueHost('Field1')).not.toThrow();
        expect(vh9).toBeNull();
        let vh10: ICalcValueHost | null = null;
        expect(() => vh10 = testItem.getCalcValueHost('Field2')).not.toThrow();
        expect(vh10).toBeInstanceOf(CalcValueHost);
        let vh11: IStaticValueHost | null = null;
        expect(() => vh11 = testItem.getStaticValueHost('Field1')).not.toThrow();
        expect(vh11).toBeInstanceOf(StaticValueHost);
        let vh12: IStaticValueHost | null = null;
        expect(() => vh12 = testItem.getStaticValueHost('Field2')).not.toThrow();
        expect(vh12).toBeNull();

    });
    test('When supplying an unknown ValueHostName, return null.', () => {

        let config1: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            validatorConfigs: null,
        };

        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false),
            valueHostConfigs: [config1]
        });
        let vh1: IValueHost | null = null;
        expect(() => vh1 = testItem.getValueHost('Unknown')).not.toThrow();
        expect(vh1).toBeNull();
        let vh2: IValidatorsValueHostBase | null = null;
        expect(() => vh2 = testItem.getValidatorsValueHost('Unknown')).not.toThrow();
        expect(vh1).toBeNull();
        let vh3: IFieldValueHost | null = null;
        expect(() => vh3 = testItem.getFieldValueHost('Unknown')).not.toThrow();
        expect(vh3).toBeNull();
        let vh4: ICalcValueHost | null = null;
        expect(() => vh4 = testItem.getCalcValueHost('Unknown')).not.toThrow();
        expect(vh4).toBeNull();
        let vh5: IStaticValueHost | null = null;
        expect(() => vh5 = testItem.getStaticValueHost('Unknown')).not.toThrow();
        expect(vh5).toBeNull();
    });

    // getFieldByElementIdentifier(elementIdentifier: string): IFieldValueHost | null
    test('getFieldByElementIdentifier returns the expected FieldValueHost when both have elementIdentifier established', () =>
    {
        let config1: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            elementIdentifier: 'A',
            validatorConfigs: null,
        };
        let config2: FieldValueHostConfig = {
            name: 'Field2',
            valueHostType: ValueHostType.Field,
            label: 'Field 2',
            elementIdentifier: 'B',
            validatorConfigs: null,
        };
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false),
            valueHostConfigs: [config1, config2]
        });
        let vh1: IFieldValueHost | null = testItem.getFieldByElementIdentifier('A');
        expect(vh1).toBeInstanceOf(FieldValueHost);
        expect(vh1!.getName()).toBe('Field1');
        let vh2: IFieldValueHost | null = testItem.getFieldByElementIdentifier('B');
        expect(vh2).toBeInstanceOf(FieldValueHost);
        expect(vh2!.getName()).toBe('Field2');
        // no match returns null
        let vh3: IFieldValueHost | null = testItem.getFieldByElementIdentifier('C');
        expect(vh3).toBeNull();
    });
    // getFieldByPropertyName(propertyName: string): IFieldValueHost | null
    test('getFieldByPropertyName returns the expected FieldValueHost when both have propertyName established', () =>
    {
        let config1: FieldValueHostConfig = {
            name: 'Field1',
            valueHostType: ValueHostType.Field,
            label: 'Field 1',
            propertyName: 'A',
            validatorConfigs: null,
        };
        let config2: FieldValueHostConfig = {
            name: 'Field2',
            valueHostType: ValueHostType.Field,
            label: 'Field 2',
            propertyName: 'B',
            validatorConfigs: null,
        };
        let testItem = new Publicify_ValueHostsManager({
            services: new MockJivsServices(false, false),
            valueHostConfigs: [config1, config2]
        });
        let vh1: IFieldValueHost | null = testItem.getFieldByPropertyName('A');
        expect(vh1).toBeInstanceOf(FieldValueHost);
        expect(vh1!.getName()).toBe('Field1');
        let vh2: IFieldValueHost | null = testItem.getFieldByPropertyName('B');
        expect(vh2).toBeInstanceOf(FieldValueHost);
        expect(vh2!.getName()).toBe('Field2');
        // no match returns null
        let vh3: IFieldValueHost | null = testItem.getFieldByPropertyName('C');
        expect(vh3).toBeNull();
    });    
});

// validate(group?: string): Array<ValueHostValidateResult>
// get isValid(): boolean
// doNotSave: boolean
// getIssuesForField(valueHostName: ValueHostName): Array<IssueFound>
// getIssuesFound(group?: string): Array<IssueFound>
describe('validate, and isValid, doNotSave, getIssuesForField, getIssuesFound based on the results', () => {
    test('Before calling validate with 0 fieldValueHosts, isValid=true, doNotSave=false, getIssuesForField=[], getIssuesFound=[]', () => {
        let setup = setupValueHostsManager();
        expect(setup.valueHostsManager.isValid).toBe(true);
        expect(setup.valueHostsManager.doNotSave).toBe(false);
        expect(setup.valueHostsManager.getIssuesForField('Anything')).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });
    test('isValid is true and doNotSave is false before calling validate with 1 fieldValueHosts', () => {
        let config = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType]);
        let setup = setupValueHostsManager([config]);
        expect(setup.valueHostsManager.isValid).toBe(true);
        expect(setup.valueHostsManager.doNotSave).toBe(false);
        expect(setup.valueHostsManager.getIssuesForField(config.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });
    test('With 1 fieldValueHost that is ValidationStatus.Valid, returns {isValid:true, doNotSave: false, issuesFound: null}', () => {
        let config = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType]);
        let setup = setupValueHostsManager([config]);

        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate()).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });

        expect(setup.valueHostsManager.isValid).toBe(true);
        expect(setup.valueHostsManager.doNotSave).toBe(false);
        expect(setup.valueHostsManager.getIssuesForField(config.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });
    test('With 1 fieldValueHost that is ValidationStatus.Invalid, returns {isValid:false, doNotSave: true, issuesFound: [1 found] }', () => {

        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config]);

        let expectedIssueFound: IssueFound = {
            errorCode: NeverMatchesConditionType,
            valueHostName: 'Field1',
            errorMessage: 'Error 1: ' + NeverMatchesConditionType,
            summaryMessage: 'Summary 1: ' + NeverMatchesConditionType,
            severity: ValidationSeverity.Error,
            doNotSave: true
        };

        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate()).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: false,
            doNotSave: true,
            issuesFound: [expectedIssueFound],
            asyncProcessing: false
        });


        expect(setup.valueHostsManager.isValid).toBe(false);
        expect(setup.valueHostsManager.doNotSave).toBe(true);

        expect(setup.valueHostsManager.getIssuesForField(config.name)).toEqual([expectedIssueFound]);
        expect(setup.valueHostsManager.getIssuesFound()).toEqual([expectedIssueFound]);
    });

    test('With 1 fieldValueHost with 2 validators, one Match the other NoMatch, returns {isValid:false, doNotSave: true, issuesFound: [1 found] }', () => {
        let config = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType, NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config]);

        let expectedIssueFound: IssueFound = {
            errorCode: NeverMatchesConditionType,
            valueHostName: 'Field1',
            errorMessage: 'Error 1: ' + NeverMatchesConditionType,
            summaryMessage: 'Summary 1: ' + NeverMatchesConditionType,
            severity: ValidationSeverity.Error,
            doNotSave: true
        };

        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate()).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: false,
            doNotSave: true,
            issuesFound: [expectedIssueFound],
            asyncProcessing: false
        });

        expect(setup.valueHostsManager.isValid).toBe(false);
        expect(setup.valueHostsManager.doNotSave).toBe(true);

        expect(setup.valueHostsManager.getIssuesForField(config.name)).toEqual([expectedIssueFound]);
        expect(setup.valueHostsManager.getIssuesFound()).toEqual([expectedIssueFound]);
    });

    test('With 2 fieldValueHost that are both valid, returns {isValid:true, doNotSave: false, issuesFound: null}', () => {

        let config1 = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType]);
        let config2 = setupFieldValueHostConfig(1, [AlwaysMatchesConditionType]);

        let setup = setupValueHostsManager([config1, config2]);

        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate()).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });

        expect(setup.valueHostsManager.isValid).toBe(true);
        expect(setup.valueHostsManager.doNotSave).toBe(false);

        expect(setup.valueHostsManager.getIssuesForField(config1.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesForField(config2.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });

    test('With 2 fieldValueHost that are both undetermined, returns {isValid:true, doNotSave: false, issuesFound: null}', () => {

        let config1 = setupFieldValueHostConfig(0, [IsUndeterminedConditionType]);
        let config2 = setupFieldValueHostConfig(1, [IsUndeterminedConditionType]);

        let setup = setupValueHostsManager([config1, config2]);

        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate()).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });

        expect(setup.valueHostsManager.isValid).toBe(true);
        expect(setup.valueHostsManager.doNotSave).toBe(false);
        expect(setup.valueHostsManager.getIssuesForField(config1.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesForField(config2.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });
    test('With 2 fieldValueHost that are both Invalid, returns {isValid:false, doNotSave: true, issuesFound: [2 entries]}', () => {

        let config1 = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let config2 = setupFieldValueHostConfig(1, [NeverMatchesConditionType]);

        let setup = setupValueHostsManager([config1, config2]);
        let expectedIssueFound: IssueFound = {
            errorCode: NeverMatchesConditionType,
            valueHostName: 'Field1',
            errorMessage: 'Error 1: ' + NeverMatchesConditionType,
            summaryMessage: 'Summary 1: ' + NeverMatchesConditionType,
            severity: ValidationSeverity.Error,
            doNotSave: true
        };
        let expectedIssueFound2: IssueFound = {
            errorCode: NeverMatchesConditionType,
            valueHostName: 'Field2',
            errorMessage: 'Error 2: ' + NeverMatchesConditionType,
            summaryMessage: 'Summary 2: ' + NeverMatchesConditionType,
            severity: ValidationSeverity.Error,
            doNotSave: true
        };

        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate()).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: false,
            doNotSave: true,
            issuesFound: [expectedIssueFound, expectedIssueFound2],
            asyncProcessing: false
        });

        expect(setup.valueHostsManager.isValid).toBe(false);
        expect(setup.valueHostsManager.doNotSave).toBe(true);

        expect(setup.valueHostsManager.getIssuesForField(config1.name)).toEqual([expectedIssueFound]);
        expect(setup.valueHostsManager.getIssuesForField(config2.name)).toEqual([expectedIssueFound2]);

        expect(setup.valueHostsManager.getIssuesFound()).toEqual([expectedIssueFound, expectedIssueFound2]);
    });
    test('With 1 external IssueFound not associated with any ValueHost, isValid=false, DoNotSave=true, getIssuesFound has the external IssueFound, and there is a new ValueHost for the BusinessLogic', () => {
        let setup = setupValueHostsManager();
        let result = setup.valueHostsManager.addExternalIssuesFound([
            {
                errorMessage: 'BL_ERROR'
            }
        ], true);
        expect(result).toBe(true);
        expect(setup.valueHostsManager.isValid).toBe(false);
        expect(setup.valueHostsManager.doNotSave).toBe(true);
        expect(setup.valueHostsManager.getIssuesFound()).toEqual([<IssueFound>{
            errorMessage: 'BL_ERROR',
            severity: ValidationSeverity.Error,
            valueHostName: ModelValidatorsValueHostName,
            errorCode: 'GENERATED_0',
            summaryMessage: 'BL_ERROR',
            doNotSave: true
        }]);
        expect(setup.valueHostsManager.getValueHost(ModelValidatorsValueHostName)).toBeInstanceOf(ModelValidatorsValueHost);

        expect(setup.valueHostsManager.getIssuesForField(ModelValidatorsValueHostName)).toEqual([<IssueFound>{
            errorMessage: 'BL_ERROR',
            severity: ValidationSeverity.Error,
            valueHostName: ModelValidatorsValueHostName,
            errorCode: 'GENERATED_0',
            summaryMessage: 'BL_ERROR',
            doNotSave: true
        }]);
    });
    test('With 1 ValueHost that is assigned without validators 1 external IssueFound, isValid=false, DoNotSave=true, getIssuesFound has the external IssueFound, and there is a no ValueHost for the BusinessLogic', () => {

        let config = setupFieldValueHostConfig(0, []);
        let setup = setupValueHostsManager([config]);
        let result = setup.valueHostsManager.addExternalIssuesFound([
            {
                errorMessage: 'BL_ERROR',
                valueHostName: config.name
            }
        ], true);
        expect(result).toBe(true);
        expect(setup.valueHostsManager.isValid).toBe(false);
        expect(setup.valueHostsManager.doNotSave).toBe(true);
        expect(setup.valueHostsManager.getIssuesFound()).toEqual([<IssueFound>{
            errorMessage: 'BL_ERROR',
            severity: ValidationSeverity.Error,
            valueHostName: config.name,
            errorCode: 'GENERATED_0',
            summaryMessage: 'BL_ERROR',
            doNotSave: true
        }]);
        expect(setup.valueHostsManager.getIssuesForField(config.name)).toEqual([<IssueFound>{
            errorMessage: 'BL_ERROR',
            severity: ValidationSeverity.Error,
            valueHostName: config.name,
            errorCode: 'GENERATED_0',
            summaryMessage: 'BL_ERROR',
            doNotSave: true
        }]);

        expect(setup.valueHostsManager.getValueHost(ModelValidatorsValueHostName)).toBeNull();
        expect(setup.valueHostsManager.getIssuesForField(ModelValidatorsValueHostName)).toBeNull();
    });
    test('With 1 ValueHost that is assigned with 1 validator that is NoMatch, 1 external IssueFound not associated with a ValueHost, isValid=false, DoNotSave=true, getIssuesFound has both errors external IssueFound, BLValueHost has the BLError, FieldValueHost has its own error', () => {

        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        config.validatorConfigs![0].errorMessage = 'CONDITION ERROR';
        config.validatorConfigs![0].summaryMessage = 'SUMMARY CONDITION ERROR';
        let setup = setupValueHostsManager([config]);
        let result = setup.valueHostsManager.addExternalIssuesFound([
            {
                errorMessage: 'BL_ERROR',
            }
        ], true);
        expect(result).toBe(true);
        setup.valueHostsManager.validate();
        expect(setup.valueHostsManager.isValid).toBe(false);
        expect(setup.valueHostsManager.doNotSave).toBe(true);
        expect(setup.valueHostsManager.getIssuesFound()).toEqual([
            <IssueFound>{
                errorMessage: 'CONDITION ERROR',
                severity: ValidationSeverity.Error,
                valueHostName: config.name,
                errorCode: NeverMatchesConditionType,
                summaryMessage: 'SUMMARY CONDITION ERROR',
                doNotSave: true
            },
            <IssueFound>{
                errorMessage: 'BL_ERROR',
                severity: ValidationSeverity.Error,
                valueHostName: ModelValidatorsValueHostName,
                errorCode: 'GENERATED_0',
                summaryMessage: 'BL_ERROR',
                doNotSave: true
            }]);
        expect(setup.valueHostsManager.getIssuesForField(config.name)).toEqual([<IssueFound>{
            errorMessage: 'CONDITION ERROR',
            severity: ValidationSeverity.Error,
            valueHostName: config.name,
            errorCode: NeverMatchesConditionType,
            summaryMessage: 'SUMMARY CONDITION ERROR',
            doNotSave: true
        }]);

        expect(setup.valueHostsManager.getValueHost(ModelValidatorsValueHostName)).toBeInstanceOf(ModelValidatorsValueHost);
        expect(setup.valueHostsManager.getIssuesForField(ModelValidatorsValueHostName)).toEqual(
            [<IssueFound>{
                errorMessage: 'BL_ERROR',
                severity: ValidationSeverity.Error,
                valueHostName: ModelValidatorsValueHostName,
                errorCode: 'GENERATED_0',
                summaryMessage: 'BL_ERROR',
                doNotSave: true
            }]);
    });
    test('addExternalIssuesFound has not supplied any errors and returns false', () => {
        let setup = setupValueHostsManager();
        let result = setup.valueHostsManager.addExternalIssuesFound([], true);
        expect(result).toBe(false);
    });
    test('addExternalIssuesFound called twice. First time has changes. Second not, but both return true because the second changes by clearing the first', () => {
        let setup = setupValueHostsManager();
        let result = setup.valueHostsManager.addExternalIssuesFound([
            {
                errorMessage: 'BL_ERROR',
            }
        ], true);
        expect(result).toBe(true);
        let result2 = setup.valueHostsManager.addExternalIssuesFound([], true);
        expect(result2).toBe(true);
    });
    test('OnValidated callback test invokes callback with expected ValidationState', () => {
        let callbackValue: ValidationState | null = null;
        let callback = (vhm: IValueHostsManager, validationState: ValidationState) => {
            callbackValue = validationState
        };
        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config], null, {
            onValidationStateChanged: callback
        });
        let expectedIssueFound: IssueFound = {
            errorCode: 'GENERATED_0',
            errorMessage: 'BL_ERROR',
            summaryMessage: 'BL_ERROR',
            severity: ValidationSeverity.Error,
            valueHostName: ModelValidatorsValueHostName,
            doNotSave: true
        };

        setup.valueHostsManager.addExternalIssuesFound([
            {
                errorMessage: 'BL_ERROR'
            }
        ], true);
        expect(callbackValue).toEqual(<ValidationState>{
            isValid: false,
            doNotSave: true,
            issuesFound: [expectedIssueFound],
            asyncProcessing: false
        });

    });
    test('OnValidated callback test with option.OmitCallback=true does not invoke callback', () => {
        let callbackValue: ValidationState | null = null;
        let callback = (vhm: IValueHostsManager, validationState: ValidationState) => {
            callbackValue = validationState
        };
        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config], null, {
            onValidationStateChanged: callback
        });
        let expectedIssueFound: IssueFound = {
            errorCode: 'GENERATED_0',
            errorMessage: 'BL_ERROR',
            severity: ValidationSeverity.Error,
            valueHostName: ModelValidatorsValueHostName,
            doNotSave: true,
        };

        setup.valueHostsManager.addExternalIssuesFound([
            {
                errorMessage: 'BL_ERROR'
            }
        ], true, { skipCallback: true });
        expect(callbackValue).toBeNull();

    });
    test('With 1 fieldValueHost and a condition that will evaluate as NoMatch, use option Preliminary=true, expect ValidationStatus.Undetermined because Require should be skipped, leaving NO validators', () => {
        const conditionType = 'TEST';
        let config = setupFieldValueHostConfig(0, [conditionType]);
        let setup = setupValueHostsManager([config]);
        (setup.services.conditionFactory as ConditionFactory).register<UserSuppliedResultConditionConfig>(
            conditionType, (config) => new UserSuppliedResultCondition({
                conditionType: conditionType,
                category: ConditionCategory.Require,
                result: ConditionEvaluateResult.NoMatch
            }));

        (setup.valueHostsManager.getValueHost('Field1')! as IFieldValueHost).setTextValue('');

        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate({ preliminary: true })).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });

        expect(setup.valueHostsManager.isValid).toBe(true);
        expect(setup.valueHostsManager.doNotSave).toBe(false);
        expect(setup.valueHostsManager.getIssuesForField(config.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });
    test('With 1 fieldValueHost and a condition that will evaluate as NoMatch, use option Preliminary=false, expect ValidationStatus.Invalid because Preliminary is off', () => {
        const conditionType = 'TEST';
        let config = setupFieldValueHostConfig(0, [conditionType]);
        let setup = setupValueHostsManager([config]);
        (setup.services.conditionFactory as ConditionFactory).register<UserSuppliedResultConditionConfig>(
            conditionType, (config) => new UserSuppliedResultCondition({
                conditionType: conditionType,
                category: ConditionCategory.Require,
                result: ConditionEvaluateResult.NoMatch
            }));
        let expectedIssueFound: IssueFound = {
            errorCode: conditionType,
            valueHostName: 'Field1',
            errorMessage: 'Error 1: ' + conditionType,
            summaryMessage: 'Summary 1: ' + conditionType,
            severity: ValidationSeverity.Severe, // only because Require conditions default to Severe
            doNotSave: true
        };

        (setup.valueHostsManager.getValueHost('Field1')! as IFieldValueHost).setValue('');
        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate({ preliminary: false })).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: false,
            doNotSave: true,
            issuesFound: [expectedIssueFound],
            asyncProcessing: false
        });

        expect(setup.valueHostsManager.isValid).toBe(false);
        expect(setup.valueHostsManager.doNotSave).toBe(true);

        expect(setup.valueHostsManager.getIssuesForField(config.name)).toEqual([expectedIssueFound]);

        expect(setup.valueHostsManager.getIssuesFound()).toEqual([expectedIssueFound]);
    });
    test('With 1 fieldValueHost and a condition that will evaluate as NoMatch during Edit, use option DuringEdit=true, expect Invalid', () => {
        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config]);
        let expectedIssueFound: IssueFound = {
            errorCode: NeverMatchesConditionType,
            valueHostName: 'Field1',
            errorMessage: 'Error 1: ' + NeverMatchesConditionType,
            summaryMessage: 'Summary 1: ' + NeverMatchesConditionType,
            severity: ValidationSeverity.Error,
            doNotSave: true
        };

        (setup.valueHostsManager.getValueHost('Field1')! as IFieldValueHost).setTextValue('');
        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate({ duringEdit: true })).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: false,
            doNotSave: true,
            issuesFound: [expectedIssueFound],
            asyncProcessing: false
        });

    });
    test('With 1 fieldValueHost and a condition that will evaluate as NoMatch during edit, use option DuringEdit=true, expect Invalid', () => {
        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config]);
        let expectedIssueFound: IssueFound = {
            errorCode: NeverMatchesConditionType,
            valueHostName: 'Field1',
            errorMessage: 'Error 1: ' + NeverMatchesConditionType,
            summaryMessage: 'Summary 1: ' + NeverMatchesConditionType,
            severity: ValidationSeverity.Error,
            doNotSave: true
        };

        setup.valueHostsManager.getFieldValueHost('Field1')?.setTextValue(''); // requires text for duringEdit
        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate({ duringEdit: true })).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: false,
            doNotSave: true,
            issuesFound: [expectedIssueFound],
            asyncProcessing: false
        });
    });
    test('With 1 fieldValueHost that has a string value and a condition that will evaluate as Match during edit, use option DuringEdit=true, expect Valid', () => {
        let config = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType]);
        let setup = setupValueHostsManager([config]);
        setup.valueHostsManager.getFieldValueHost(config.name)?.setTextValue('Text');
        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate({ duringEdit: true })).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });
    });
    test('With 1 fieldValueHost that has an undefined value and a condition that will evaluate as Match during edit, use option DuringEdit=true, expect Undetermined because DuringEdit requires a string value', () => {
        let config = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType]);
        let setup = setupValueHostsManager([config]);

        setup.valueHostsManager.getFieldValueHost(config.name)?.setTextValue(undefined);
        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate({ duringEdit: true })).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });

    });
    test('With 1 fieldValueHost and a condition that will evaluate as NoMatch, use option DuringEdit=false, expect normal Invalid as DuringEdit has no impact on Category=Require validators', () => {
        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config]);
        let expectedIssueFound: IssueFound = {
            errorCode: NeverMatchesConditionType,
            valueHostName: 'Field1',
            errorMessage: 'Error 1: ' + NeverMatchesConditionType,
            summaryMessage: 'Summary 1: ' + NeverMatchesConditionType,
            severity: ValidationSeverity.Error,
            doNotSave: true
        };

        (setup.valueHostsManager.getValueHost('Field1')! as IFieldValueHost).setValue('');

        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate({ duringEdit: false })).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: false,
            doNotSave: true,
            issuesFound: [expectedIssueFound],
            asyncProcessing: false
        });

    });
    test('With 1 fieldValueHost and a condition that does not implement IEvaluateConditionDuringEdits, use option DuringEdit=true, expect condition to be skipped and array of ValidateResults = []', () => {
        let config = setupFieldValueHostConfig(0, [UserSuppliedResultConditionType]);
        (config.validatorConfigs![0].conditionConfig as UserSuppliedResultConditionConfig).result = ConditionEvaluateResult.Match;
        let setup = setupValueHostsManager([config]);

        let vh = (setup.valueHostsManager.getValueHost('Field1')! as IFieldValueHost);
        vh.setTextValue('');

        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate({ duringEdit: true })).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });

    });
    test('With 1 fieldValueHost and a NeverMatch condition that will evaluate as NoMatch, use option DuringEdit=false, expect normal Invalid as DuringEdit=false has no impact on including validators', () => {
        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config]);
        let expectedIssueFound: IssueFound = {
            errorCode: NeverMatchesConditionType,
            valueHostName: 'Field1',
            errorMessage: 'Error 1: ' + NeverMatchesConditionType,
            summaryMessage: 'Summary 1: ' + NeverMatchesConditionType,
            severity: ValidationSeverity.Error,
            doNotSave: true
        };

        (setup.valueHostsManager.getValueHost('Field1')! as IFieldValueHost).setValue('');

        let validationState: ValidationState | null = null;
        expect(() => validationState = setup.valueHostsManager.validate({ duringEdit: false })).not.toThrow();
        expect(validationState).toEqual(<ValidationState>{
            isValid: false,
            doNotSave: true,
            issuesFound: [expectedIssueFound],
            asyncProcessing: false
        });
    });
    test('OnValidated callback test', () => {
        let callbackValue: ValidationState | null = null;
        let callback = (vhm: IValueHostsManager, validationState: ValidationState) => {
            callbackValue = validationState
        };
        let config = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType]);
        let setup = setupValueHostsManager([config], null, {
            onValidationStateChanged: callback
        });

        let validationState = setup.valueHostsManager.validate();

        expect(callbackValue).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });
    });
    test('OnValidated callback test with skipCallback does not callback', () => {
        let callbackValue: ValidationState | null = null;
        let callback = (vhm: IValueHostsManager, validationState: ValidationState) => {
            callbackValue = validationState
        };
        let config = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType]);
        let setup = setupValueHostsManager([config], null, {
            onValidationStateChanged: callback
        });

        let validationState = setup.valueHostsManager.validate({ skipCallback: true });

        expect(callbackValue).toBeNull();
    });
    test('When ValueHost.isEnabled=false, even though validator is valid, result is as if the validator did not get used because its own ValidationStatus=Disabled', () => {
        let config = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType]);
        let setup = setupValueHostsManager([config]);
        let vh = setup.valueHostsManager.getFieldValueHost(config.name)!;
        vh.setEnabled(false);
        setup.valueHostsManager.validate();
        expect(vh.validationStatus).toBe(ValidationStatus.Disabled);
        expect(setup.valueHostsManager.isValid).toBe(true);
        expect(setup.valueHostsManager.doNotSave).toBe(false);
        expect(setup.valueHostsManager.getIssuesForField(config.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });
    test('When ValueHost.isEnabled=false, even though validator is invalid, result is as if the validator did not get used because its own ValidationStatus=Disabled', () => {
        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config]);
        let vh = setup.valueHostsManager.getFieldValueHost(config.name)!;
        vh.setEnabled(false);
        setup.valueHostsManager.validate();
        expect(vh.validationStatus).toBe(ValidationStatus.Disabled);
        expect(setup.valueHostsManager.isValid).toBe(true);
        expect(setup.valueHostsManager.doNotSave).toBe(false);
        expect(setup.valueHostsManager.getIssuesForField(config.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });
    test('validate() passes final ValidationState to callback', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{
            services: services,
            valueHostConfigs: [],
            onValidationStateChanged:
                (valueHostsManager: IValueHostsManager, validationState: ValidationState) => callbackValidationState = validationState
        };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        // let vmConfig = { services: services, valueHostConfigs: [] };
        // builder.onValidationStateChanged = 
        //     (valueHostsManager: IValueHostsManager, validationState: ValidationState) => callbackValidationState = validationState;
        // builder.field('Field1', LookupKey.String).requireText('required');
        // let valConfig = builder.complete();

        let callbackValidationState: ValidationState | null = null;
        let testItem = new ValueHostsManager(vmConfig);
        testItem.getFieldValueHost('Field1')!.setValues('', '');

        let validationState = testItem.validate();

        expect(callbackValidationState).toBe(validationState);
        expect(callbackValidationState).not.toBeNull();
        expect(callbackValidationState!.issuesFound?.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required')).toBe(true);
    });

    test('validate({ skipCallback: true }) suppresses callback', () => {
        let callbackValidationState: ValidationState | null = null;
        let services = createJivsServicesForTesting();
        let vmConfig = {
            services: services,
            valueHostConfigs: [{
                valueHostType: ValueHostType.Field,
                name: 'Field1',
                dataType: LookupKey.String,
                validatorConfigs: [{
                    errorMessage: 'required',
                    conditionConfig: {
                        conditionType: ConditionType.RequireText        
                    }
                }]
            }],
            onValidationStateChanged:
                (valueHostsManager: IValueHostsManager, validationState: ValidationState) => callbackValidationState = validationState
        };

        let testItem = new ValueHostsManager(vmConfig);
        testItem.getFieldValueHost('Field1')!.setValues('', '');

        let validationState = testItem.validate({
            skipCallback: true
        });

        expect(validationState).not.toBeNull();
        expect(callbackValidationState).toBeNull();
    });

    test('validate({ group }) filters issuesFound to that group', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType: LookupKey.String,
            group: 'A',
            validatorConfigs: [{
                errorMessage: 'required A',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field2',
            dataType: LookupKey.String,
            group: 'B',
            validatorConfigs: [{
                errorMessage: 'required B',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        let testItem = new ValueHostsManager(vmConfig);
        testItem.getFieldValueHost('Field1')!.setValues('', '');
        testItem.getFieldValueHost('Field2')!.setValues('', '');

        let validationState = testItem.validate({ group: 'A' });

        expect(validationState.issuesFound).not.toBeNull();
        expect(validationState.issuesFound?.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required A')).toBe(true);
        expect(validationState.issuesFound?.some((x) => x.valueHostName === 'Field2' && x.errorMessage === 'required B')).toBe(false);
    });

    test('validate({ group }) reflects group specific isValid, doNotSave and asyncProcessing', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType: LookupKey.String,
            group: 'A',
            validatorConfigs: [{
                errorMessage: 'required A',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field2',
            dataType: LookupKey.String,
            group: 'B',
            validatorConfigs: [{
                errorMessage: 'required B',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });
        // builder.field('Field1', LookupKey.String, { group: 'A' }).requireText('required A');
        // builder.field('Field2', LookupKey.String, { group: 'B' }).requireText('required B');
        // let valConfig = builder.complete();

        let testItem = new ValueHostsManager(vmConfig);
        testItem.getFieldValueHost('Field1')!.setValues('ok', 'ok');
        testItem.getFieldValueHost('Field2')!.setValues('', '');

        let validationState = testItem.validate({ group: 'A' });
        let validationState2 = testItem.validate({ group: 'B' });

        expect(validationState.issuesFound == null || validationState.issuesFound.length === 0).toBe(true);
        expect(validationState.isValid).toBe(true);
        expect(validationState.doNotSave).toBe(false);
        expect(validationState.asyncProcessing).toBe(false);

        expect(validationState2.issuesFound != null && validationState2.issuesFound.length === 1).toBe(true);
        expect(validationState2.isValid).toBe(false);
        expect(validationState2.doNotSave).toBe(true);
        expect(validationState2.asyncProcessing).toBe(false);
    });

    test('validate() excludes disabled hosts from validation and aggregate state', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType: LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required 1',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field2',
            dataType: LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required 2',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });
        let valConfig = vmConfig;

        let testItem = new ValueHostsManager(valConfig);
        testItem.getFieldValueHost('Field1')!.setValues('', '');
        testItem.getFieldValueHost('Field2')!.setValues('', '');
        testItem.getFieldValueHost('Field2')!.setEnabled(false);

        let validationState = testItem.validate();

        expect(validationState.issuesFound).not.toBeNull();
        expect(validationState.issuesFound?.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required 1')).toBe(true);
        expect(validationState.issuesFound?.some((x) => x.valueHostName === 'Field2' && x.errorMessage === 'required 2')).toBe(false);
    });


    test('returns only issues for the requested group', () => {
        let configA = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        configA.group = 'A';
        let configB = setupFieldValueHostConfig(1, [NeverMatchesConditionType2]);
        configB.group = 'B';

        let setup = setupValueHostsManager([configA, configB]);

        setup.valueHostsManager.validate();

        expect(setup.valueHostsManager.getIssuesFound('A')).toEqual([
            expect.objectContaining({ errorCode: NeverMatchesConditionType })
        ]);
        expect(setup.valueHostsManager.getIssuesFound('B')).toEqual([
            expect.objectContaining({ errorCode: NeverMatchesConditionType2 })
        ]);
    });

    test('returns null when no issues match the requested group', () => {
        let configA = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        configA.group = 'A';
        let configB = setupFieldValueHostConfig(1, null);
        configB.group = 'B';

        let setup = setupValueHostsManager([configA, configB]);

        setup.valueHostsManager.validate({ group: 'A' });

        expect(setup.valueHostsManager.getIssuesFound('B')).toBeNull();
    });

    test('returns null when there are no issues at all for the requested group', () => {
        let configA = setupFieldValueHostConfig(0, null);
        configA.group = 'A';
        let configB = setupFieldValueHostConfig(1, null);
        configB.group = 'B';

        let setup = setupValueHostsManager([configA, configB]);

        setup.valueHostsManager.validate();

        expect(setup.valueHostsManager.getIssuesFound('A')).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound('B')).toBeNull();
    });

    test('getIssuesFound(group) matches group names case-insensitively', () => {
        let configA = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        configA.group = 'GroupA';
        let configB = setupFieldValueHostConfig(1, [NeverMatchesConditionType2]);
        configB.group = 'GroupB';

        let setup = setupValueHostsManager([configA, configB]);

        setup.valueHostsManager.validate();

        expect(setup.valueHostsManager.getIssuesFound('groupa')).toEqual([
            expect.objectContaining({ errorCode: NeverMatchesConditionType })
        ]);
        expect(setup.valueHostsManager.getIssuesFound('GROUPB')).toEqual([
            expect.objectContaining({ errorCode: NeverMatchesConditionType2 })
        ]);
    });

    test('getIssuesFound("*") behaves the same as getIssuesFound() with no group filter', () => {
        let configA = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        configA.group = 'A';
        let configB = setupFieldValueHostConfig(1, [NeverMatchesConditionType2]);
        configB.group = 'B';

        let setup = setupValueHostsManager([configA, configB]);

        setup.valueHostsManager.validate();

        expect(setup.valueHostsManager.getIssuesFound('*')).toEqual(
            setup.valueHostsManager.getIssuesFound()
        );
    });

    test('validate() returns isValid=true and doNotSave=false when a host needs validation but is skipped by a non-matching group', () => {
        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config]);
        let vh = setup.valueHostsManager.getValueHost(config.name) as IValidatableValueHostBase;
        vh.setValue('test', { validate: false });    // should set validation status to NeedsValidation
        expect(vh.currentValidationState.status).toBe(ValidationStatus.NeedsValidation);
        // validate() returns ValidationState for VM. Do avoid changing vh's state
        // we must call it with a group that does not match.

        let validationState = setup.valueHostsManager.validate({ group: 'nonMatchingGroup' });
        expect(vh.currentValidationState.status).toBe(ValidationStatus.NeedsValidation);
        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });

    });

    test('validate(group:B) returns the correct results for group b only which isValid=true, doNotSave=false', () => {
        let configA = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        configA.group = 'A';
        let configB = setupFieldValueHostConfig(1, [AlwaysMatchesConditionType]);
        configB.group = 'B';
        let setup = setupValueHostsManager([configA, configB]);
        let vhA = setup.valueHostsManager.getValueHost(configA.name) as IValidatableValueHostBase;
        vhA.setValue('testA', { validate: false });    // should set validation status to NeedsValidation
        let vhB = setup.valueHostsManager.getValueHost(configB.name) as IValidatableValueHostBase;
        vhB.setValue('testB', { validate: false });    // should set validation status to NeedsValidation

        let validationState = setup.valueHostsManager.validate({ group: 'B' });
        expect(vhA.currentValidationState.status).toBe(ValidationStatus.NeedsValidation);
        expect(vhB.currentValidationState.status).toBe(ValidationStatus.Valid);

        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });

    });

    test('validate(group:B) returns the correct results for group b only which isValid=false, doNotSave=true', () => {
        let configA = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        configA.group = 'A';
        let configB = setupFieldValueHostConfig(1, [NeverMatchesConditionType2]);
        configB.group = 'B';
        let setup = setupValueHostsManager([configA, configB]);
        let vhA = setup.valueHostsManager.getValueHost(configA.name) as IValidatableValueHostBase;
        vhA.setValue('testA', { validate: false });    // should set validation status to NeedsValidation
        let vhB = setup.valueHostsManager.getValueHost(configB.name) as IValidatableValueHostBase;
        vhB.setValue('testB', { validate: false });    // should set validation status to NeedsValidation

        let validationState = setup.valueHostsManager.validate({ group: 'B' });
        expect(vhA.currentValidationState.status).toBe(ValidationStatus.NeedsValidation);
        expect(vhB.currentValidationState.status).toBe(ValidationStatus.Invalid);

        expect(validationState).toEqual(<ValidationState>{
            isValid: false,
            doNotSave: true,
            issuesFound: [
                {
                    'valueHostName': vhB.getName(),
                    'doNotSave': true,
                    'errorCode': NeverMatchesConditionType2,
                    'errorMessage': 'Error 2: ' + NeverMatchesConditionType2,
                    'severity': ValidationSeverity.Error,
                    'summaryMessage': 'Summary 2: ' + NeverMatchesConditionType2,
                }
            ],
            asyncProcessing: false
        });
    });
    test('Two ValueHosts that have warning validators results with 2 issues but ValidationState.doNotSave=false', () => {
        let configA = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        configA.validatorConfigs![0].severity = ValidationSeverity.Warning;
        let configB = setupFieldValueHostConfig(1, [NeverMatchesConditionType2]);
        configB.validatorConfigs![0].severity = ValidationSeverity.Warning;
        let setup = setupValueHostsManager([configA, configB]);
        let vhA = setup.valueHostsManager.getValueHost(configA.name) as IValidatableValueHostBase;
        vhA.setValue('testA', { validate: false });    // should set validation status to NeedsValidation
        let vhB = setup.valueHostsManager.getValueHost(configB.name) as IValidatableValueHostBase;
        vhB.setValue('testB', { validate: false });    // should set validation status to NeedsValidation

        let validationState = setup.valueHostsManager.validate();
        expect(vhA.currentValidationState.status).toBe(ValidationStatus.Valid);
        expect(vhA.currentValidationState.doNotSave).toBe(false);
        expect(vhB.currentValidationState.status).toBe(ValidationStatus.Valid);
        expect(vhB.currentValidationState.doNotSave).toBe(false);

        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: [
                {
                    'valueHostName': vhA.getName(),
                    'doNotSave': false,
                    'errorCode': NeverMatchesConditionType,
                    'errorMessage': 'Error 1: ' + NeverMatchesConditionType,
                    'severity': ValidationSeverity.Warning,
                    'summaryMessage': 'Summary 1: ' + NeverMatchesConditionType,
                },
                {
                    'valueHostName': vhB.getName(),
                    'doNotSave': false,
                    'errorCode': NeverMatchesConditionType2,
                    'errorMessage': 'Error 2: ' + NeverMatchesConditionType2,
                    'severity': ValidationSeverity.Warning,
                    'summaryMessage': 'Summary 2: ' + NeverMatchesConditionType2,
                }
            ],
            asyncProcessing: false
        });
    });
    test('getIssuesForField returns null for an existing host that is not a ValidatableValueHostBase', () => {
        let services = createJivsServicesForTesting();
        let cf = services.conditionFactory as ConditionFactory;
        cf.register<RegExpConditionConfig>(ConditionType.RegExp, (config) => new RegExpCondition(config));

        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: []
        });
        vmConfig.valueHostConfigs.push(<StaticValueHostConfig>{
            valueHostType: ValueHostType.Static,
            name: 'Field2',
            dataType:  LookupKey.Number
        });
        // let builder = new ValueHostsManagerConfigBuilder(services);
        // builder.field('Field1', LookupKey.String);
        // builder.static('Field2', LookupKey.Number);
        let vhm = new ValueHostsManager(vmConfig);

        vhm.validate();

        expect(vhm.getIssuesForField('Field2')).toBeNull();
    });
});

describe('clearValidation', () => {
    test('With 2 fieldValueHost that are both Invalid, returns 2 ValidateResults each with 1 issue found. isValid=false. DoNotSave=true', () => {

        let config1 = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let config2 = setupFieldValueHostConfig(1, [NeverMatchesConditionType]);

        let setup = setupValueHostsManager([config1, config2]);

        setup.valueHostsManager.validate();
        expect(() => setup.valueHostsManager.clearValidation()).not.toThrow();
        expect(setup.valueHostsManager.isValid).toBe(true);
        expect(setup.valueHostsManager.doNotSave).toBe(false);
        expect(setup.valueHostsManager.getIssuesForField(config1.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesForField(config2.name)).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });

    // ValueHostsManager.validate() itself will call onValidate immediately while
    // each of the child ValueHosts will call it using debounce.
    //
    function testOnValidatedWithDebounce(delayConfig: number | undefined,
        expectedOnValidatedCalls: number,
        useVHCallback: boolean, numberOfValueHosts: number = 1, delayAfterVHCallback: number = 100): void {
        jest.useFakeTimers();

        let capturedFromOnValidated: Array<ValidationState> = [];
        let callbackValidated = (vhm: IValueHostsManager, validationState: ValidationState) => {
            capturedFromOnValidated.push(validationState);
        };
        let capturedFromOnValueHostValidated: Array<ValueHostValidationState> = [];
        let callbackValueHostValidated = (valueHost: IValidatableValueHostBase, validationState: ValueHostValidationState) => {
            capturedFromOnValueHostValidated.push(validationState);
            jest.advanceTimersByTime(delayAfterVHCallback); // has the potential to trigger onValidate
        };
        let ivConfigs: Array<FieldValueHostConfig> = [];
        for (let i = 0; i < numberOfValueHosts; i++)
            ivConfigs.push(setupFieldValueHostConfig(i, [NeverMatchesConditionType]));
        let setup = setupValueHostsManager(ivConfigs, null, {
            onValidationStateChanged: callbackValidated,
            onValueHostValidationStateChanged: useVHCallback ? callbackValueHostValidated : undefined,
            notifyValidationStateChangedDelay: delayConfig
        });
        setup.services.autoGenerateDataTypeCheckService.enabled = false;

        setup.valueHostsManager.validate();

        expect(capturedFromOnValidated.length).toBe(expectedOnValidatedCalls);
        if (useVHCallback)
            expect(capturedFromOnValueHostValidated.length).toBe(numberOfValueHosts);
    }
    test('with notifyValidationStateChangedDelay=0, OnValidated invoked twice because debounce is off and ValueHost.validate fires it once', () => {
        testOnValidatedWithDebounce(0, 2, false);
    });
    test('with notifyValidationStateChangedDelay=undefined, OnValidated invoked once because debounce is on', () => {
        testOnValidatedWithDebounce(undefined, 1, false);
    });
    test('with notifyValidationStateChangedDelay=1000, OnValidated invoked once because debounce is on', () => {
        testOnValidatedWithDebounce(1000, 1, false);
    });
    test('With ValueHostcallbacks enabled and no delay after, and with notifyValidationStateChangedDelay=0, OnValidated invoked twice because debounce is off', () => {
        testOnValidatedWithDebounce(0, 2, true, 1, 0);
        testOnValidatedWithDebounce(0, 3, true, 2, 0);
    });
    test('With ValueHostcallbacks enabled and no delay after, and with notifyValidationStateChangedDelay=undefined, OnValidated invoked once', () => {
        testOnValidatedWithDebounce(undefined, 1, true, 1, 0);
        testOnValidatedWithDebounce(undefined, 1, true, 2, 0);
    });
    test('With ValueHostcallbacks enabled and no delay after, and with notifyValidationStateChangedDelay=1000, OnValidated invoked once', () => {
        testOnValidatedWithDebounce(1000, 1, true, 1, 0);
        testOnValidatedWithDebounce(1000, 1, true, 2, 0);
    });
    test('With ValueHostcallbacks enabled and delay after < overall delay, and with notifyValidationStateChangedDelay=1000, OnValidated invoked once', () => {
        testOnValidatedWithDebounce(1000, 1, true, 1, 1000 - 1);
        testOnValidatedWithDebounce(1000, 1, true, 2, 1000 - 1);
    });
    test('With ValueHostcallbacks enabled and delay after > overall delay, and with notifyValidationStateChangedDelay=1000, OnValidated invoked twice as debounce triggers', () => {
        testOnValidatedWithDebounce(1000, 2, true, 1, 1000 + 1);
        testOnValidatedWithDebounce(1000, 3, true, 2, 1000 + 1);
    });
    test('OnValidated callback test with option.OmitCallback=true does not invoke callback', () => {
        let callbackValue: ValidationState | null = null;
        let callback = (vhm: IValueHostsManager, validationState: ValidationState) => {
            callbackValue = validationState
        };
        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let setup = setupValueHostsManager([config], null, {
            onValidationStateChanged: callback
        });

        let validationState = setup.valueHostsManager.validate({ skipCallback: true });
        expect(callbackValue).toBeNull();

        setup.valueHostsManager.clearValidation({ skipCallback: true });
        expect(callbackValue).toBeNull();
    });

    test('returns false when there is no validation state to clear', () => {
        let config = createValidatableValueHostBaseConfig(1);
        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);

        let changed = setup.valueHostsManager.clearValidation();

        expect(changed).toBe(false);
    });

    test('returns false when a group is supplied and no matching host has validation state', () => {

        let configA = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        configA.group = 'A';
        let configB = setupFieldValueHostConfig(1, null);   // has no validators
        configB.group = 'B';

        let setup = setupValueHostsManager([configA, configB]);

        setup.valueHostsManager.validate({ group: 'A' });

        let changed = setup.valueHostsManager.clearValidation({ group: 'B' });

        expect(changed).toBe(false);
    });

    test('clearValidation({ group }) only clears validation state for the matching group', () => {
        let configA = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        configA.group = 'A';
        let configB = setupFieldValueHostConfig(1, [NeverMatchesConditionType2]);   // has no validators
        configB.group = 'B';
        let setup = setupValueHostsManager([configA, configB]);

        setup.valueHostsManager.validate();

        expect(setup.valueHostsManager.getIssuesFound()).toEqual([
            expect.objectContaining({ errorCode: NeverMatchesConditionType }),
            expect.objectContaining({ errorCode: NeverMatchesConditionType2 })
        ]);

        let changed = setup.valueHostsManager.clearValidation({ group: 'A' });

        expect(changed).toBe(true);
        expect(setup.valueHostsManager.getIssuesFound()).toEqual([
            expect.objectContaining({ errorCode: NeverMatchesConditionType2 })
        ]);
        expect(setup.valueHostsManager.getIssuesFound('A')).toBeNull();
        expect(setup.valueHostsManager.getIssuesFound('B')).toEqual([
            expect.objectContaining({ errorCode: NeverMatchesConditionType2 })
        ]);
    });
    test('clearValidation({ group: "*" }) behaves the same as clearValidation() with no group filter', () => {
        let configA = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        configA.group = 'A';
        let configB = setupFieldValueHostConfig(1, [NeverMatchesConditionType2]);
        configB.group = 'B';

        let setup = setupValueHostsManager([configA, configB]);

        setup.valueHostsManager.validate();

        let changed = setup.valueHostsManager.clearValidation({ group: '*' });

        expect(changed).toBe(true);
        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });
});

describe('asyncProcessing', () => {
    test('validate() returns asyncProcessing=true when one validatable host is async', () => {
        let config = createValidatableValueHostBaseConfig(1);
        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);
        let host = setup.valueHostsManager.getValueHost(config.name) as TestValidatableValueHost;

        host.setValidateWillReturn(ValidationStatus.Valid);
        host.setAsyncProcess(true);

        let validationState = setup.valueHostsManager.validate();

        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: true,
            issuesFound: null,
            asyncProcessing: true
        });
        expect(setup.valueHostsManager.asyncProcessing).toBe(true);
    });

    test('validate() returns asyncProcessing=true when one of multiple validatable hosts is async', () => {
        let config1 = createValidatableValueHostBaseConfig(1);
        let config2 = createValidatableValueHostBaseConfig(2);
        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config1, config2]);

        let host1 = setup.valueHostsManager.getValueHost(config1.name) as TestValidatableValueHost;
        let host2 = setup.valueHostsManager.getValueHost(config2.name) as TestValidatableValueHost;

        host1.setValidateWillReturn(ValidationStatus.Valid);
        host2.setValidateWillReturn(ValidationStatus.Valid);
        host2.setAsyncProcess(true);

        let validationState = setup.valueHostsManager.validate();

        expect(validationState).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: true,
            issuesFound: null,
            asyncProcessing: true
        });
        expect(setup.valueHostsManager.asyncProcessing).toBe(true);
    });

    test('validate({ group }) only reports asyncProcessing for hosts in the requested group', () => {
        let config1 = {
            ...createValidatableValueHostBaseConfig(1),
            group: 'A'
        };
        let config2 = {
            ...createValidatableValueHostBaseConfig(2),
            group: 'B'
        };
        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config1, config2]);

        let host1 = setup.valueHostsManager.getValueHost(config1.name) as TestValidatableValueHost;
        let host2 = setup.valueHostsManager.getValueHost(config2.name) as TestValidatableValueHost;

        host1.setValidateWillReturn(ValidationStatus.Valid);
        host2.setValidateWillReturn(ValidationStatus.Valid);
        host1.setAsyncProcess(true);

        let validationStateA = setup.valueHostsManager.validate({ group: 'A' });
        let validationStateB = setup.valueHostsManager.validate({ group: 'B' });

        expect(validationStateA).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: true,
            issuesFound: null,
            asyncProcessing: true
        });
        expect(validationStateB).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });
    });

    test('clearValidation() clears manager asyncProcessing after an async host was validated', () => {
        let config = createValidatableValueHostBaseConfig(1);
        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);
        let host = setup.valueHostsManager.getValueHost(config.name) as TestValidatableValueHost;

        host.setValidateWillReturn(ValidationStatus.Valid);
        host.setAsyncProcess(true);

        let validationState = setup.valueHostsManager.validate();

        expect(validationState.asyncProcessing).toBe(true);
        expect(setup.valueHostsManager.asyncProcessing).toBe(true);

        let changed = setup.valueHostsManager.clearValidation();

        expect(changed).toBe(true);
        expect(setup.valueHostsManager.asyncProcessing).toBe(false);

        let validationStateAfterClear = setup.valueHostsManager.validate();

        expect(validationStateAfterClear).toEqual(<ValidationState>{
            isValid: true,
            doNotSave: false,
            issuesFound: null,
            asyncProcessing: false
        });
    });
});

// updateState(updater: (stateToUpdate: TState) => TState): TState
describe('updateState', () => {
    interface ITestExtendedState extends ValueHostsManagerInstanceState {
        Value: number;
    }
    function testUpdateState(initialValue: number, testCallback: (stateToUpdate: ITestExtendedState) => ITestExtendedState,
        callback: ((valueHost: IValueHost, instanceState: ValueHostInstanceState) => void) | null): Array<ITestExtendedState>
    {

        let config = setupFieldValueHostConfig(0, [NeverMatchesConditionType]);
        let state: ITestExtendedState = {
            Value: initialValue
        };
        let setup = setupValueHostsManager([config], state);
        if (callback)
            setup.valueHostsManager.onNotifyValueHostInstanceStateChanged = callback as any;
        let testItem = setup.valueHostsManager as unknown as Publicify_ValueHostsManager<ITestExtendedState>;
        let changes: Array<ITestExtendedState> = [];
        let fn = (stateToUpdate: ITestExtendedState): ITestExtendedState => {
            let lastValue = stateToUpdate.Value;
            stateToUpdate = testCallback(stateToUpdate);
            if (lastValue !== stateToUpdate.Value)
                changes.push(stateToUpdate);
            return stateToUpdate;
        };

        // try several times
        for (let i = 1; i <= 3; i++) {
            expect(() => testItem.updateInstanceState(fn)).not.toThrow();
        }
        return changes;
    }
    test('Update value with +1 results in new instance of State',
        () => {
            let testCallback = (stateToUpdate: ITestExtendedState): ITestExtendedState => {
                stateToUpdate.Value = stateToUpdate.Value + 1;
                return stateToUpdate;
            };
            const initialValue = 100;
            let changes = testUpdateState(initialValue, testCallback, null);
            expect(changes.length).toBe(3);
            for (let i = 1; i <= 3; i++) {
                expect(changes[i - 1].Value).toBe(initialValue + i);
            }

        });
    test('Update value with +0 results in no change to the state instance',
        () => {
            let testCallback = (stateToUpdate: ITestExtendedState): ITestExtendedState => {
                return stateToUpdate;
            };
            const initialValue = 100;
            let changes = testUpdateState(initialValue, testCallback, null);
            expect(changes.length).toBe(0);
        });
    test('Update value with +1 results in new instance of State and report thru updateState',
        () => {
            let testCallback = (stateToUpdate: ITestExtendedState): ITestExtendedState => {
                stateToUpdate.Value = stateToUpdate.Value + 1;
                return stateToUpdate;
            };
            const initialValue = 100;

            let changes = testUpdateState(initialValue, testCallback, null);
            expect(changes.length).toBe(3);
            for (let i = 1; i <= 3; i++) {
                expect(changes[i - 1].Value).toBe(initialValue + i);
            }

        });
    test('Update value with +0 results in no change to the state instance nor seen in updateState',
        () => {
            let testCallback = (stateToUpdate: ITestExtendedState): ITestExtendedState => {
                return stateToUpdate;
            };
            const initialValue = 100;
            let onStateChanges: Array<ITestExtendedState> = [];
            let changes = testUpdateState(initialValue, testCallback, (vhm, state) => {
                onStateChanges.push(state as unknown as ITestExtendedState);
            });
            expect(changes.length).toBe(0);
        });
    test('Updater function is null throws',
        () => {
            let setup = setupValueHostsManager();
            let testItem = setup.valueHostsManager as unknown as Publicify_ValueHostsManager<ITestExtendedState>;
            expect(() => testItem.updateInstanceState(null!)).toThrow(/updater/);
        });
});
describe('vh', () => {
    test('Returns an instance of ValueHostAccessor', () => {
        let vhConfig: ValueHostsManagerConfig = {
            services: createJivsServicesForTesting(),
            valueHostConfigs: [{
                valueHostType: ValueHostType.Static,
                name: 'Field1'
            }]
        };
        let vhm = new ValueHostsManager(vhConfig);
        expect(vhm.vh).toBeInstanceOf(ValueHostAccessor);
        expect(vhm.vh.static('Field1')).toBeInstanceOf(StaticValueHost);
        expect(() => vhm.vh.calc('X')).toThrow(/unknown/);
        expect(() => vhm.vh.calc('Field1')).toThrow(/CalcValueHost/);
        expect(() => vhm.vh.field('Field1')).toThrow(/FieldValueHost/);
    });
});
describe('enumerateValueHosts', () => {
    test('No valuehosts exist, returns generator.result.done=true on first request', () => {
        let vhConfig: ValueHostsManagerConfig = {
            services: createJivsServicesForTesting(),
            valueHostConfigs: []
        };
        let vhm = new ValueHostsManager(vhConfig);
        let generator = vhm.enumerateValueHosts();
        expect(generator).toBeDefined();
        let first = generator.next();
        expect(first.done).toBe(true);
        expect(first.value).toBeUndefined();
    });
    test('3 valuehosts exist, returns all 3 and on the 4th call, returns result.done=true', () => {
        let vhConfig: ValueHostsManagerConfig = {
            services: createJivsServicesForTesting(),
            valueHostConfigs: [{
                valueHostType: ValueHostType.Static,
                name: 'Field1'
            },
            <CalcValueHostConfig>{
                valueHostType: ValueHostType.Calc,
                name: 'Field2',
                calcFn: () => 0
            },
            {
                valueHostType: ValueHostType.Static,
                name: 'Field3'
            }]
        };
        let vhm = new ValueHostsManager(vhConfig);
        let generator = vhm.enumerateValueHosts();
        expect(generator).toBeDefined();
        let first = generator.next();
        expect(first.done).toBe(false);
        expect(first.value).toBeInstanceOf(StaticValueHost);
        let second = generator.next();
        expect(second.done).toBe(false);
        expect(second.value).toBeInstanceOf(CalcValueHost);
        let third = generator.next();
        expect(third.done).toBe(false);
        expect(third.value).toBeInstanceOf(StaticValueHost);
        let fourth = generator.next();
        expect(fourth.done).toBe(true);
        expect(fourth.value).toBeUndefined();
    });
    test('3 valuehosts exist and filter wants only StaticValueHost, returns 1 StaticValueHost and on the 2nd call, returns result.done=true', () => {
        let vhConfig: ValueHostsManagerConfig = {
            services: createJivsServicesForTesting(),
            valueHostConfigs: [{
                valueHostType: ValueHostType.Static,
                name: 'Field1'
            },
            <CalcValueHostConfig>{
                valueHostType: ValueHostType.Calc,
                name: 'Field2',
                calcFn: () => 0
            }]
        };
        let vhm = new ValueHostsManager(vhConfig);
        let generator = vhm.enumerateValueHosts((vh) => vh instanceof StaticValueHost);
        expect(generator).toBeDefined();
        let first = generator.next();
        expect(first.done).toBe(false);
        expect(first.value).toBeInstanceOf(StaticValueHost);
        let second = generator.next();
        expect(second.done).toBe(true);
        expect(second.value).toBeUndefined();
    });
    test('3 valuehosts exist and filter wants only CalcValueHost, returns result.done=true on first request', () => {
        let vhConfig: ValueHostsManagerConfig = {
            services: createJivsServicesForTesting(),
            valueHostConfigs: [{
                valueHostType: ValueHostType.Static,
                name: 'Field1'
            },
            {
                valueHostType: ValueHostType.Static,
                name: 'Field2'
            },
            {
                valueHostType: ValueHostType.Static,
                name: 'Field3'
            }]
        };
        let vhm = new ValueHostsManager(vhConfig);
        let generator = vhm.enumerateValueHosts((vh) => vh instanceof CalcValueHost);
        expect(generator).toBeDefined();
        let first = generator.next();
        expect(first.done).toBe(true);
        expect(first.value).toBeUndefined();
    });
});

describe('invokeOnConfigChanged', () => {
    function testCallback(configs: Array<ValueHostConfig>): void {
        function handler(manager: IValueHostsManager, configs: Array<ValueHostConfig>): void {
            configsReceived = configs;
        }
        let configsReceived: Array<ValueHostConfig> | undefined = undefined;
        let vmConfig: ValueHostsManagerConfig = {
            services: new MockJivsServices(true, false),
            valueHostConfigs: configs,
            onConfigChanged: handler
        };
        let testItem = new Publicify_ValueHostsManager(vmConfig);
        testItem.publicify_invokeOnConfigChanged();
        expect(configsReceived).toEqual(configs);
        // confirm all configs are not the same instances as those that are held in Manager
        configsReceived!.forEach(config => {
            expect(testItem.publicify_valueHostConfigs.get(config.name)).not.toBe(config);
        });

    }
    test('Callback not connected does not throw', () => {
        let vmConfig: ValueHostsManagerConfig = {
            services: new MockJivsServices(true, false), valueHostConfigs: []
        };
        let testItem = new Publicify_ValueHostsManager(vmConfig);
        expect(() => testItem.publicify_invokeOnConfigChanged()).not.toThrow();
    });
    test('With no ValueHostConfigs, callback still invoked and returns an empty array', () => {
        testCallback([]);
    });
    test('With several ValueHostConfigs, callback still invoked and returns the identical configs', () => {
        testCallback([{
            valueHostType: ValueHostType.Static,
            name: 'Field1'
        },
        {
            valueHostType: ValueHostType.Static,
            name: 'Field2'
        }
        ]);
    });
    test('ValueHostManager constructor does not invoke when it adds ValueHosts from ValueHostsConfig', () => {
        function handler(manager: IValueHostsManager, configs: Array<ValueHostConfig>): void {
            configsReceived = configs;
        }
        let configsReceived: Array<ValueHostConfig> | undefined = undefined;
        let config1: StaticValueHostConfig = {
            valueHostType: ValueHostType.Static,
            name: 'Field1'
        };
        let config2: StaticValueHostConfig = {
            valueHostType: ValueHostType.Static,
            name: 'Field2',
            dataType: LookupKey.String,
            label: 'Field 2'
        };
        let vmConfig: ValueHostsManagerConfig = {
            services: new MockJivsServices(true, false),
            valueHostConfigs: [config1, config2],
            onConfigChanged: handler
        };
        let testItem = new Publicify_ValueHostsManager(vmConfig);

        expect(configsReceived).toBeUndefined();
    });
    test('Use addValueHost to invoke', () => {
        function handler(manager: IValueHostsManager, configs: Array<ValueHostConfig>): void {
            configsReceived = configs;
        }
        let configsReceived: Array<ValueHostConfig> | undefined = undefined;
        let vmConfig: ValueHostsManagerConfig = {
            services: new MockJivsServices(true, false),
            valueHostConfigs: [],
            onConfigChanged: handler
        };
        let testItem = new Publicify_ValueHostsManager(vmConfig);
        let config1: StaticValueHostConfig = {
            valueHostType: ValueHostType.Static,
            name: 'Field1'
        };
        testItem.addValueHost(config1, null);
        expect(configsReceived).toEqual([config1]);
        let config2: StaticValueHostConfig = {
            valueHostType: ValueHostType.Static,
            name: 'Field2'
        };
        testItem.addValueHost(config2, null);
        expect(configsReceived).toEqual([config1, config2]);
    });
    test('Use addOrUpdateValueHost to invoke, with both referencing the same ValueHostName', () => {
        function handler(manager: IValueHostsManager, configs: Array<ValueHostConfig>): void {
            configsReceived = configs;
        }
        let configsReceived: Array<ValueHostConfig> | undefined = undefined;
        let vmConfig: ValueHostsManagerConfig = {
            services: new MockJivsServices(true, false),
            valueHostConfigs: [],
            onConfigChanged: handler
        };
        let testItem = new Publicify_ValueHostsManager(vmConfig);
        let config1: StaticValueHostConfig = {
            valueHostType: ValueHostType.Static,
            name: 'Field1'
        };
        testItem.addOrUpdateValueHost(config1, null);
        expect(configsReceived).toEqual([config1]);
        let config2: StaticValueHostConfig = {
            valueHostType: ValueHostType.Static,
            name: 'Field1',
            dataType: LookupKey.String,
            label: 'Field 1'
        };
        testItem.addOrUpdateValueHost(config2, null);
        expect(configsReceived).toEqual([config2]);
    });
    test('Use addOrMergeValueHost to invoke, with both referencing the same ValueHostName', () => {
        function handler(manager: IValueHostsManager, configs: Array<ValueHostConfig>): void {
            configsReceived = configs;
        }
        let configsReceived: Array<ValueHostConfig> | undefined = undefined;
        let vmConfig: ValueHostsManagerConfig = {
            services: new MockJivsServices(true, false),
            valueHostConfigs: [],
            onConfigChanged: handler
        };
        let testItem = new Publicify_ValueHostsManager(vmConfig);
        let config1: StaticValueHostConfig = {
            valueHostType: ValueHostType.Static,
            name: 'Field1',
            dataType: LookupKey.String
        };
        testItem.addOrMergeValueHost(config1, null);
        expect(configsReceived).toEqual([config1]);
        let config2: StaticValueHostConfig = {
            valueHostType: ValueHostType.Static,
            name: 'Field1',
            label: 'Field 1'
        };
        testItem.addOrMergeValueHost(config2, null);
        expect(configsReceived).toEqual([<StaticValueHostConfig>{
            valueHostType: ValueHostType.Static,
            name: 'Field1',
            label: 'Field 1',
            dataType: LookupKey.String
        }]);
    });
    test('Use discardValueHost to invoke', () => {
        function handler(manager: IValueHostsManager, configs: Array<ValueHostConfig>): void {
            configsReceived = configs;
        }
        let configsReceived: Array<ValueHostConfig> | undefined = undefined;
        let config1: StaticValueHostConfig = {
            valueHostType: ValueHostType.Static,
            name: 'Field1'
        };
        let config2: StaticValueHostConfig = {
            valueHostType: ValueHostType.Static,
            name: 'Field2',
            dataType: LookupKey.String,
            label: 'Field 2'
        };
        let vmConfig: ValueHostsManagerConfig = {
            services: new MockJivsServices(true, false),
            valueHostConfigs: [config1, config2],
            onConfigChanged: handler
        };
        let testItem = new Publicify_ValueHostsManager(vmConfig);

        testItem.discardValueHost('Field1');
        expect(configsReceived).toEqual([config2]);
        // discardValueHost with an unknown field does not invoke the function
        configsReceived = undefined;
        testItem.discardValueHost('Field1');
        expect(configsReceived).toBeUndefined();
    });
});
describe('dispose', () => {
    test('dispose kills all expected properties', () => {
        let vmConfig: ValueHostsManagerConfig = {
            services: new MockJivsServices(true, false),
            valueHostConfigs: [{
                valueHostType: ValueHostType.Static,
                name: 'Field1'
            }]
        };
        // let builder = new ValueHostsManagerConfigBuilder(vmConfig);
        // builder.static('Field1');
        let testItem = new Publicify_ValueHostsManager(vmConfig);
        let vh = testItem.vh.static('Field1');
        testItem.dispose();
        expect(testItem.publicify_InstanceState).toBeUndefined();
        expect(testItem.publicify_valueHostConfigs).toBeUndefined();
        expect(testItem.publicify_valueHosts).toBeUndefined();

        expect(() => testItem.getValueHost('Field1')).toThrow(TypeError);
        expect(() => vh.getDataType()).toThrow(TypeError);
        expect(() => vh.setValue(0)).toThrow(TypeError);

    });
    test('dispose with valueHostsConfig having its own dispose kills what the config.dispose expects', () => {
        interface X extends ValueHostsManagerConfig, IDisposable {
            x: {}
        }
        let vmConfig: X = {
            services: new MockJivsServices(true, false), valueHostConfigs: [],
            x: {},
            dispose: () => { vmConfig.x = undefined! }
        };

        vmConfig.valueHostConfigs.push(<StaticValueHostConfig>{
            valueHostType: ValueHostType.Static,
            name: 'Field1'
        });

        // let builder = new ValueHostsManagerConfigBuilder(vmConfig);
        // builder.static('Field1');
        let testItem = new Publicify_ValueHostsManager(vmConfig);
        testItem.dispose();
        expect(vmConfig.x).toBeUndefined();

    });
    test('Ordinary does not make any noise', () => {
        let config = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType]);
        let setup = setupValueHostsManager([config]);
        expect(() => setup.valueHostsManager.dispose()).not.toThrow();
    });
    test('Ordinary does not make any noise', () => {
        jest.useFakeTimers();
        let config = setupFieldValueHostConfig(0, [AlwaysMatchesConditionType]);
        let countCalls = 0;
        let setup = setupValueHostsManager([config], null, {
            onValidationStateChanged: (vhm, vs) => {
                countCalls++;
            },
            notifyValidationStateChangedDelay: 100
        });
        setup.services.autoGenerateDataTypeCheckService.enabled = false;
        setup.valueHostsManager.notifyValidationStateChanged(null);   // starts a debounce delay
        jest.advanceTimersByTime(100);  // force onValidationStateChanged to run
        expect(countCalls).toBe(1);

        setup.valueHostsManager.notifyValidationStateChanged(null);   // repeat, starts a debounce delay
        setup.valueHostsManager.dispose();
        jest.advanceTimersByTime(100);  // force onValidationStateChanged to run   
        expect(countCalls).toBe(1);
    });
});

describe('toIValueHostsManagerCallbacks function', () => {
    test('Matches interface returns strongly typed object.', () => {
        let testItem: IValueHostsManagerCallbacks = {
            onValueChanged: (vh: IValueHost, old: any) => { },
            onTextValueChanged: (vh: IValidatableValueHostBase, old: any) => { },
            onValueHostValidationStateChanged: (vh: IValidatableValueHostBase, snapshot: ValueHostValidationState) => { },
            onValidationStateChanged: (vhm, results) => { },
            onConfigChanged: (vhm, config) => { }
        };
        expect(toIValueHostsManagerCallbacks(testItem)).toBe(testItem);
    });
    test('ValueHostsManager without callbacks defined returns itself.', () => {
        let testItem = new ValueHostsManager({
            services: new JivsServices(),
            valueHostConfigs: []
        });
        expect(toIValueHostsManagerCallbacks(testItem)).toBe(testItem);
    });
    test('ValueHostsManager with callbacks defined returns itself.', () => {
        let testItem = new ValueHostsManager({
            services: new JivsServices(),
            valueHostConfigs: [],
            onValueChanged: (vh: IValueHost, old: any) => { },
            onTextValueChanged: (vh: IValidatableValueHostBase, old: any) => { },
            onValueHostValidationStateChanged: (vh: IValidatableValueHostBase, snapshot: ValueHostValidationState) => { },
            onValidationStateChanged: (vhm, results) => { },
            onConfigChanged: (vhm, config) => { }
        });
        expect(toIValueHostsManagerCallbacks(testItem)).toBe(testItem);
    });
    test('ValueHostsManager with callbacks=null defined returns itself.', () => {
        let testItem = new ValueHostsManager({
            services: new JivsServices(),
            valueHostConfigs: [],
            onValueChanged: null,
            onTextValueChanged: null,
            onValueHostValidationStateChanged: null,
            onValidationStateChanged: null,
            onConfigChanged: null
        });
        expect(toIValueHostsManagerCallbacks(testItem)).toBe(testItem);
    });
    test('Non-matching interface returns null.', () => {
        let testItem = {};
        expect(toIValueHostsManagerCallbacks(testItem)).toBeNull();
    });
    test('null returns null.', () => {
        expect(toIValueHostsManagerCallbacks(null)).toBeNull();
    });
    test('Non-object returns null.', () => {
        expect(toIValueHostsManagerCallbacks(100)).toBeNull();
    });
});

describe('toIValueHostResolver function', () => {
    test('Matches interface returns strongly typed object.', () => {
        let testItem: IValueHostResolver = {
            dispose: () => { },
            getValueHost: (name) => { return <any>{}; },
            vh: {} as unknown as IValueHostAccessor,
            getCalcValueHost: (name) => { return <any>{}; },
            getStaticValueHost: (name) => { return <any>{}; },
            services: new MockJivsServices(false, false),
            enumerateValueHosts: function (): Generator<IValueHost, any, unknown> {
                throw new Error("Function not implemented.");
            }
        };
        expect(toIValueHostResolver(testItem)).toBe(testItem);
    });
    test('ValueHostsManager matches and returns itself.', () => {
        let testItem = new ValueHostsManager<ValueHostsManagerInstanceState>({
            services: new JivsServices(),
            valueHostConfigs: []
        });
        expect(toIValueHostResolver(testItem)).toBe(testItem);
    });
    test('Non-matching interface returns null.', () => {
        let testItem = {};
        expect(toIValueHostResolver(testItem)).toBeNull();
    });
    test('null returns null.', () => {
        expect(toIValueHostResolver(null)).toBeNull();
    });
    test('Non-object returns null.', () => {
        expect(toIValueHostResolver(100)).toBeNull();
    });
});
describe('toIValueHostsManager function', () => {
    test('Matches interface returns strongly typed object.', () => {
        let testItem: IValueHostsManager = {
            validate: function (options?: ValidateOptions | undefined): ValidationState
            {
                throw new Error("Function not implemented.");
            },
            clearValidation: function (options?: ValidateOptions | undefined): boolean
            {
                throw new Error("Function not implemented.");
            },
            isValid: false,
            doNotSave: true,
            behaviors: <Behaviors> {
                activeCultureId: 'en-US',
            },

            getIssuesForField: function (valueHostName: string): IssueFound[] | null
            {
                throw new Error("Function not implemented.");
            },
            getIssuesFound: function (group?: string | undefined): IssueFound[] | null
            {
                throw new Error("Function not implemented.");
            },

            notifyValidationStateChanged: function (validationState: ValidationState | null, options?: ValidateOptions | undefined, force?: boolean | undefined): void
            {
                throw new Error("Function not implemented.");
            },
            dispose: function (): void
            {
                throw new Error("Function not implemented.");
            },
            notifyOtherValueHostsOfValueChange: function (valueHostIdThatChanged: string, revalidate: boolean): void
            {
                throw new Error("Function not implemented.");
            },
            getValueHost: function (valueHostName: string): IValueHost | null
            {
                throw new Error("Function not implemented.");
            },
            vh: {} as unknown as IValueHostAccessor,
            getValidatorsValueHost(valueHostName: ValueHostName): IValidatorsValueHostBase | null
            {
                throw new Error("Function not implemented.");
            },
            getFieldValueHost: function (valueHostName: string): IFieldValueHost | null
            {
                throw new Error("Function not implemented.");
            },
            getCalcValueHost: function (valueHostName: string): ICalcValueHost | null
            {
                throw new Error("Function not implemented.");
            },
            getStaticValueHost: function (valueHostName: string): IStaticValueHost | null
            {
                throw new Error("Function not implemented.");
            },
            services: new MockJivsServices(false, false),
            addValueHost: function (config: ValueHostConfig, initialState: ValueHostInstanceState | null): IValueHost
            {
                throw new Error("Function not implemented.");
            },
            addOrUpdateValueHost: function (config: ValueHostConfig, initialState: ValueHostInstanceState | null): IValueHost
            {
                throw new Error("Function not implemented.");
            },
            addOrMergeValueHost(config: ValueHostConfig, initialState: ValueHostInstanceState | null): IValueHost
            {
                throw new Error("Function not implemented.");

            },
            discardValueHost: function (valueHostName: string): void
            {
                throw new Error("Function not implemented.");
            },
            enumerateValueHosts: function (filter?: (valueHost: IValueHost) => boolean): Generator<IValueHost>
            {
                throw new Error('Function not implemented.');
            },

            getFieldByElementIdentifier(elementIdentifier: string): IFieldValueHost | null
            {
                throw new Error('Function not implemented.');
            },

            getFieldByPropertyName(propertyName: string): IFieldValueHost | null
            {
                throw new Error('Function not implemented.');
            },

            notifyValueHostInstanceStateChanged: function (valueHost: IValueHost, instanceState: ValueHostInstanceState): void
            {
                throw new Error('Function not implemented.');
            },
            addExternalIssuesFound: function (errors: IssueFound[] | null, determinedLocally: boolean, options?: ValidateOptions | undefined): boolean
            {
                throw new Error('Function not implemented.');
            },
            addExternalIssueFound: function (error: IssueFound, determinedLocally: boolean, options?: ValidateOptions | undefined): boolean
            {
                throw new Error('Function not implemented.');
            },
            toValidationPayload: function (externalIssues: IssueFound[] | null): string
            {
                throw new Error('Function not implemented.');
            },
            fromValidationPayload: function (payload: string, encode?: ((text: string) => string) | null | undefined): boolean
            {
                throw new Error('Function not implemented.');
            },
            captureState: function (): string
            {
                throw new Error('Function not implemented.');
            }
        };
        expect(toIValueHostsManager(testItem)).toBe(testItem);
    });
    test('ValueHostsManager returns itself.', () => {
        let testItem = new ValueHostsManager({
            services: new JivsServices(),
            valueHostConfigs: []
        });
        expect(toIValueHostsManager(testItem)).toBe(testItem);
    });

    test('Non-matching interface returns null.', () => {
        let testItem = {};
        expect(toIValueHostsManager(testItem)).toBeNull();
    });
    test('null returns null.', () => {
        expect(toIValueHostsManager(null)).toBeNull();
    });
    test('Non-object returns null.', () => {
        expect(toIValueHostsManager(100)).toBeNull();
    });
});
describe('toIValueHostsManagerAccessor function', () => {
    test('Matches interface returns strongly typed object.', () => {
        let testItem: IValueHostsManagerAccessor = {
            valueHostsManager: {
                getValueHost: (name) => { return <any> {}; },
                vh: {} as unknown as IValueHostAccessor,
                getCalcValueHost: (name) => { return <any> {}; },
                getStaticValueHost: (name) => { return <any> {}; },
                services: new MockJivsServices(false, false),
                behaviors: <Behaviors> {
                    activeCultureId: 'en-US',
                },
                notifyOtherValueHostsOfValueChange: (valueHostIdThatChanged, revalidate) => {},
                dispose: () => void {},
                addValueHost: function (config: ValueHostConfig, initialState: ValueHostInstanceState | null): IValueHost
                {
                    throw new Error("Function not implemented.");
                },
                addOrUpdateValueHost: function (config: ValueHostConfig, initialState: ValueHostInstanceState | null): IValueHost
                {
                    throw new Error("Function not implemented.");
                },
                addOrMergeValueHost(config: ValueHostConfig, initialState: ValueHostInstanceState | null): IValueHost
                {
                    throw new Error("Function not implemented.");
                },
                discardValueHost: function (valueHostName: string): void
                {
                    throw new Error("Function not implemented.");
                },

                enumerateValueHosts: function (filter?: (valueHost: IValueHost) => boolean): Generator<IValueHost>
                {
                    throw new Error("Function not implemented.");
                },
                getFieldByElementIdentifier(elementIdentifier: string): IFieldValueHost | null
                {
                    throw new Error('Function not implemented.');
                },

                getFieldByPropertyName(propertyName: string): IFieldValueHost | null
                {
                    throw new Error('Function not implemented.');
                },

                notifyValueHostInstanceStateChanged: function (valueHost: IValueHost, instanceState: ValueHostInstanceState): void
                {
                    throw new Error("Function not implemented.");
                },
                addExternalIssuesFound: function (errors: IssueFound[] | null, determinedLocally: boolean, options?: ValidateOptions | undefined): boolean
                {
                    throw new Error("Function not implemented.");
                },
                addExternalIssueFound: function (error: IssueFound, determinedLocally: boolean, options?: ValidateOptions | undefined): boolean
                {
                    throw new Error("Function not implemented.");
                },
                toValidationPayload: function (externalIssues: IssueFound[] | null): string
                {
                    throw new Error("Function not implemented.");
                },
                fromValidationPayload: function (payload: string, encode?: ((text: string) => string) | null | undefined): boolean
                {
                    throw new Error("Function not implemented.");
                },
                validate: function (options?: ValidateOptions | undefined): ValidationState
                {
                    throw new Error("Function not implemented.");
                },
                clearValidation: function (options?: ValidateOptions | undefined): boolean
                {
                    throw new Error("Function not implemented.");
                },
                isValid: false,
                doNotSave: true,
                getIssuesForField: function (valueHostName: string): IssueFound[] | null
                {
                    throw new Error("Function not implemented.");
                },
                getIssuesFound: function (group?: string | undefined): IssueFound[] | null
                {
                    throw new Error("Function not implemented.");
                },
                notifyValidationStateChanged: function (validationState: ValidationState | null, options?: ValidateOptions | undefined, force?: boolean | undefined): void
                {
                    throw new Error("Function not implemented.");
                },
                getValidatorsValueHost(valueHostName: ValueHostName): IValidatorsValueHostBase | null
                {
                    throw new Error("Function not implemented.");
                },
                getFieldValueHost: function (valueHostName: string): IFieldValueHost | null
                {
                    throw new Error("Function not implemented.");
                },
                captureState: function (): string
                {
                    throw new Error('Function not implemented.');
                }
            }
        };
        expect(toIValueHostsManagerAccessor(testItem)).toBe(testItem);
    });
    test('ValueHost matches and returns itself.', () => {
        let vhm = new MockValueHostsManager(new MockJivsServices(false, false));
        let testItem = new StaticValueHost(vhm, {
            name: 'Field1',
            label: 'Label1',
        },
            {
                name: 'Field1',
                value: undefined
            });
        expect(toIValueHostsManagerAccessor(testItem)).toBe(testItem);
    });
    test('Non-matching interface returns null.', () => {
        let testItem = {};
        expect(toIValueHostsManagerAccessor(testItem)).toBeNull();
    });
    test('null returns null.', () => {
        expect(toIValueHostsManagerAccessor(null)).toBeNull();
    });
    test('Non-object returns null.', () => {
        expect(toIValueHostsManagerAccessor(100)).toBeNull();
    });
});
describe('Round trip caching of Config and State', () => {
    // create the ValueHostsManager twice. First time, we are setting up its state by changing ValueHosts.
    // then we get its state and use it in the second ValueHostsManager. Confirm that the state is correctly restored.
    test('state is correctly restored in a new instance', () =>
    {
        let services = new MockJivsServices(true, false);
        let initialConfig: ValueHostsManagerConfig = {
            services: services,
            valueHostConfigs: [
                <FieldValueHostConfig>{
                    name: 'Field1',
                    label: 'Label1',
                    valueHostType: ValueHostType.Field,
                    validatorConfigs: [
                        {
                            conditionConfig: {
                                conditionType: ConditionType.RequireText
                            }
                        }
                    ]
                },
                <FieldValueHostConfig>{
                    name: 'Field2',
                    label: 'Label2',
                    dataType: LookupKey.Number,
                    valueHostType: ValueHostType.Field
                }
            ]
        };

        let vhm1 = new Publicify_ValueHostsManager(initialConfig);
        // will supply us with a validation error
        let valueHost1A = vhm1.getValueHost('Field1') as FieldValueHost;
        valueHost1A.setValue('');

        // valueHost1B will be updated twice so its counter = 2
        let valueHost1B = vhm1.getValueHost('Field2') as FieldValueHost;
        valueHost1B.setValue(42);
        valueHost1B.setValue(43);

        vhm1.validate();

        let savedState = vhm1.captureState();

        let secondConfig = { ...initialConfig, capturedState: savedState };

        let vhm2 = new Publicify_ValueHostsManager(secondConfig);

        let valueHost2A = vhm2.getValueHost('Field1') as FieldValueHost;
        expect(valueHost2A.getValue()).toBe('');
        const stateContainer: StateContainer = {
            jivs_state: 'internal',
            vhm: vhm2.publicify_InstanceState,
            vh: []
        };        
        valueHost2A._captureState(stateContainer);
        // check counter and validator info of FieldValueHostInstanceState
        let field1State = stateContainer.vh[0] as FieldValueHostInstanceState;
        expect(field1State.name).toBe('Field1');
        expect(field1State.changeCounter).toBe(1);
        expect(field1State.issuesFound).toBeDefined();
        expect(field1State.issuesFound?.length).toBe(1);

        let valueHost2B = vhm2.getValueHost('Field2') as FieldValueHost;
        expect(valueHost2B.getValue()).toBe(43);
        valueHost2B._captureState(stateContainer);
        let field2State = stateContainer.vh[1] as FieldValueHostInstanceState;
        expect(field2State.name).toBe('Field2');
        expect(field2State.changeCounter).toBe(2);
        expect(field2State.issuesFound).toBeNull();
    });
});
describe('addExternalIssueFound()', () => {
    test('delegates to the named enabled validatable host', () => {
        let config = {
            ...createValidatableValueHostBaseConfig(1),
            name: 'Field1',
            label: 'Field 1'
        };

        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);
        let host = setup.valueHostsManager.getValueHost('Field1') as TestValidatableValueHost;
        host.callSuperAddExternalIssueFound = false;
        host.addExternalIssueFoundReturnValue = true;

        let issue: IssueFound = {
            valueHostName: 'Field1',
            errorMessage: 'EXTERNAL_ERROR'
        };
        let options: ValidateOptions = { skipCallback: true };

        let result = setup.valueHostsManager.addExternalIssueFound(issue, true, options);

        expect(result).toBe(true);
        expect(host.addExternalIssueFoundCallCount).toBe(1);
        expect(host.lastAddExternalIssueFoundCall.issueFound).toBe(issue);
        expect(host.lastAddExternalIssueFoundCall.determinedLocally).toBe(true);
        expect(host.lastAddExternalIssueFoundCall.options).toBe(options);
        expect(setup.valueHostsManager.getValueHost(ModelValidatorsValueHostName)).toBeNull();
        expect(setup.valueHostsManager.createModelValidatorsValueHostCallCount).toBe(0);
    });

    test('with no valueHostName routes to model-level host and rewrites valueHostName', () => {
        let setup = setupValueHostsManagerForAddExternalIssueFoundTests();
        let issue: IssueFound = {
            errorMessage: 'MODEL_ERROR'
        };

        let result = setup.valueHostsManager.addExternalIssueFound(issue, true);

        let modelHost = setup.valueHostsManager.getValueHost(ModelValidatorsValueHostName) as TestValidatableValueHost;

        expect(result).toBe(true);
        expect(modelHost).toBeInstanceOf(TestValidatableValueHost);
        expect(modelHost.addExternalIssueFoundCallCount).toBe(1);
        expect(modelHost.lastAddExternalIssueFoundCall.issueFound).toBe(issue);
        expect(modelHost.lastAddExternalIssueFoundCall.determinedLocally).toBe(true);
        expect(issue.valueHostName).toBe(ModelValidatorsValueHostName);
        expect(setup.valueHostsManager.createModelValidatorsValueHostCallCount).toBe(1);
    });

    test('disabled named host reroutes to model-level host', () => {
        let config = {
            ...createValidatableValueHostBaseConfig(1),
            name: 'Field1',
            label: 'Field 1'
        };

        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);
        let fieldHost = setup.valueHostsManager.getValueHost('Field1') as TestValidatableValueHost;
        fieldHost.callSuperAddExternalIssueFound = false;
        fieldHost.setEnabled(false);

        let issue: IssueFound = {
            valueHostName: 'Field1',
            errorMessage: 'DISABLED_FIELD_ERROR'
        };

        let result = setup.valueHostsManager.addExternalIssueFound(issue, true);

        let modelHost = setup.valueHostsManager.getValueHost(ModelValidatorsValueHostName) as TestValidatableValueHost;

        expect(result).toBe(true);
        expect(fieldHost.addExternalIssueFoundCallCount).toBe(0);
        expect(modelHost).toBeInstanceOf(TestValidatableValueHost);
        expect(modelHost.addExternalIssueFoundCallCount).toBe(1);
        expect(modelHost.lastAddExternalIssueFoundCall.issueFound).toBe(issue);
        expect(issue.valueHostName).toBe(ModelValidatorsValueHostName);
        expect(setup.valueHostsManager.createModelValidatorsValueHostCallCount).toBe(1);
        let logger = setup.services.loggerService as CapturingLogger;
        expect(logger.findMessage('is disabled. Rerouting error', LoggingLevel.Warn, null)).toBeTruthy();

    });

    test('unknown named host gets rerouted to ModelValidatorsValueHost', () => {
        let setup = setupValueHostsManagerForAddExternalIssueFoundTests();
        let issue: IssueFound = {
            valueHostName: 'MissingField',
            errorMessage: 'MISSING_HOST_ERROR'
        };

        let result = setup.valueHostsManager.addExternalIssueFound(issue, true);

        let modelHost = setup.valueHostsManager.getValueHost(ModelValidatorsValueHostName) as TestValidatableValueHost;

        expect(result).toBe(true);
        expect(modelHost).toBeInstanceOf(TestValidatableValueHost);
        expect(modelHost.addExternalIssueFoundCallCount).toBe(1);
        expect(modelHost.lastAddExternalIssueFoundCall.issueFound).toBe(issue);
        expect(issue.valueHostName).toBe(ModelValidatorsValueHostName);
        expect(setup.valueHostsManager.createModelValidatorsValueHostCallCount).toBe(1);
        let logger = setup.services.loggerService as CapturingLogger;
        expect(logger.findMessage('Could not find ValueHost', LoggingLevel.Warn, null)).toBeTruthy();

    });

    test('reuses an existing model-level host', () => {
        let setup = setupValueHostsManagerForAddExternalIssueFoundTests();

        let modelHost = setup.valueHostsManager.addValueHost(
            {
                ...createValidatableValueHostBaseConfig(999),
                name: ModelValidatorsValueHostName,
                label: '*'
            },
            null
        ) as TestValidatableValueHost;

        modelHost.callSuperAddExternalIssueFound = false;

        let originalHostCount = setup.valueHostsManager.publicify_valueHosts.size;
        let issue: IssueFound = {
            errorMessage: 'MODEL_ERROR'
        };

        let result = setup.valueHostsManager.addExternalIssueFound(issue, true);

        expect(result).toBe(true);
        expect(setup.valueHostsManager.publicify_valueHosts.size).toBe(originalHostCount);
        expect(setup.valueHostsManager.getValueHost(ModelValidatorsValueHostName)).toBe(modelHost);
        expect(modelHost.addExternalIssueFoundCallCount).toBe(1);
        expect(modelHost.lastAddExternalIssueFoundCall.issueFound).toBe(issue);
        expect(issue.valueHostName).toBe(ModelValidatorsValueHostName);
        expect(setup.valueHostsManager.createModelValidatorsValueHostCallCount).toBe(0);
    });
    test('passes determinedLocally = false and options through to the target host', () => {
        let config = {
            ...createValidatableValueHostBaseConfig(1),
            name: 'Field1',
            label: 'Field 1'
        };

        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);
        let host = setup.valueHostsManager.getValueHost('Field1') as TestValidatableValueHost;
        host.callSuperAddExternalIssueFound = false;
        host.addExternalIssueFoundReturnValue = true;

        let issue: IssueFound = {
            valueHostName: 'Field1',
            errorMessage: 'EXTERNAL_ERROR'
        };
        let options: ValidateOptions = { skipCallback: true };

        let result = setup.valueHostsManager.addExternalIssueFound(issue, false, options);

        expect(result).toBe(true);
        expect(host.addExternalIssueFoundCallCount).toBe(1);
        expect(host.lastAddExternalIssueFoundCall.issueFound).toBe(issue);
        expect(host.lastAddExternalIssueFoundCall.determinedLocally).toBe(false);
        expect(host.lastAddExternalIssueFoundCall.options).toBe(options);
        expect(host.lastAddExternalIssueFoundCall.options?.skipCallback).toBe(true);
    });
});

describe('addExternalIssuesFound()', () => {
    test('delegates each issue to addExternalIssueFound with determinedLocally and options', () => {
        let config1 = {
            ...createValidatableValueHostBaseConfig(1),
            name: 'Field1',
            label: 'Field 1'
        };
        let config2 = {
            ...createValidatableValueHostBaseConfig(2),
            name: 'Field2',
            label: 'Field 2'
        };

        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config1, config2]);
        let host1 = setup.valueHostsManager.getValueHost('Field1') as TestValidatableValueHost;
        let host2 = setup.valueHostsManager.getValueHost('Field2') as TestValidatableValueHost;
        host1.callSuperAddExternalIssueFound = false;
        host2.callSuperAddExternalIssueFound = false;

        let issues: IssueFound[] = [
            {
                valueHostName: 'Field1',
                errorMessage: 'ERROR_1'
            },
            {
                valueHostName: 'Field2',
                errorMessage: 'ERROR_2'
            }
        ];
        let options: ValidateOptions = { skipCallback: true };

        setup.valueHostsManager.addExternalIssuesFound(issues, false, options);

        expect(host1.addExternalIssueFoundCallCount).toBe(1);
        expect(host1.lastAddExternalIssueFoundCall.issueFound).toBe(issues[0]);
        expect(host1.lastAddExternalIssueFoundCall.determinedLocally).toBe(false);
        expect(host1.lastAddExternalIssueFoundCall.options).toBe(options);

        expect(host2.addExternalIssueFoundCallCount).toBe(1);
        expect(host2.lastAddExternalIssueFoundCall.issueFound).toBe(issues[1]);
        expect(host2.lastAddExternalIssueFoundCall.determinedLocally).toBe(false);
        expect(host2.lastAddExternalIssueFoundCall.options).toBe(options);
    });

    test('supports a mixed list of field-level and model-level issues', () => {
        let config = {
            ...createValidatableValueHostBaseConfig(1),
            name: 'Field1',
            label: 'Field 1'
        };

        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);
        let fieldHost = setup.valueHostsManager.getValueHost('Field1') as TestValidatableValueHost;
        fieldHost.callSuperAddExternalIssueFound = false;

        let issues: IssueFound[] = [
            {
                valueHostName: 'Field1',
                errorMessage: 'FIELD_ERROR'
            },
            {
                errorMessage: 'MODEL_ERROR'
            }
        ];

        setup.valueHostsManager.addExternalIssuesFound(issues, true);

        let modelHost = setup.valueHostsManager.getValueHost(ModelValidatorsValueHostName) as TestValidatableValueHost;

        expect(fieldHost.addExternalIssueFoundCallCount).toBe(1);
        expect(fieldHost.lastAddExternalIssueFoundCall.issueFound).toBe(issues[0]);

        expect(modelHost).toBeInstanceOf(TestValidatableValueHost);
        expect(modelHost.addExternalIssueFoundCallCount).toBe(1);
        expect(modelHost.lastAddExternalIssueFoundCall.issueFound).toBe(issues[1]);
        expect(issues[1].valueHostName).toBe(ModelValidatorsValueHostName);
    });
    test('routes each issue in a mixed list to the correct host', () => {
        let config = {
            ...createValidatableValueHostBaseConfig(1),
            name: 'Field1',
            label: 'Field 1'
        };

        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);
        let fieldHost = setup.valueHostsManager.getValueHost('Field1') as TestValidatableValueHost;
        fieldHost.callSuperAddExternalIssueFound = false;

        let issues: IssueFound[] = [
            {
                valueHostName: 'Field1',
                errorMessage: 'FIELD_ERROR'
            },
            {
                errorMessage: 'MODEL_ERROR'
            }
        ];

        setup.valueHostsManager.addExternalIssuesFound(issues, true);

        let modelHost = setup.valueHostsManager.getValueHost(ModelValidatorsValueHostName) as TestValidatableValueHost;

        expect(fieldHost.addExternalIssueFoundCallCount).toBe(1);
        expect(fieldHost.lastAddExternalIssueFoundCall.issueFound).toBe(issues[0]);

        expect(modelHost).toBeInstanceOf(TestValidatableValueHost);
        expect(modelHost.addExternalIssueFoundCallCount).toBe(1);
        expect(modelHost.lastAddExternalIssueFoundCall.issueFound).toBe(issues[1]);
        expect(issues[1].valueHostName).toBe(ModelValidatorsValueHostName);
    });
});

describe('toValidationPayload()', () => {
    test('returns a serialized single IssueFound[] list for validator-generated issues', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        // let vmConfig = { services: services, valueHostConfigs: [] };
        // builder.field('Field1', LookupKey.String).requireText('required');

        let testItem = new ValueHostsManager(vmConfig);
        testItem.getFieldValueHost('Field1')!.setValues('', '');
        testItem.validate();

        let payload = testItem.toValidationPayload(null);
        let issues = JSON.parse(payload) as IssueFound[] | null;

        expect(Array.isArray(issues)).toBe(true);
        expect(issues).not.toBeNull();
        expect(issues!.length).toBe(1);
        expect(issues![0].valueHostName).toBe('Field1');
        expect(issues![0].errorMessage).toBe('required');
        expect((issues as any).externalIssuesFound).toBeUndefined();
        expect((issues as any).issuesFound).toBeUndefined();
    });

    test('includes supplied external issues in the serialized single IssueFound[] list', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        // builder.field('Field1', LookupKey.String).requireText('required');

        let testItem = new ValueHostsManager(vmConfig);
        testItem.getFieldValueHost('Field1')!.setValues('', '');
        testItem.validate();

        let externalIssue: IssueFound = {
            errorMessage: 'server-side error'
        };

        let payload = testItem.toValidationPayload([externalIssue]);
        let issues = JSON.parse(payload) as IssueFound[] | null;

        expect(Array.isArray(issues)).toBe(true);
        expect(issues).not.toBeNull();
        expect(issues!.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required')).toBe(true);
        expect(issues!.some((x) => x.valueHostName === ModelValidatorsValueHostName && x.errorMessage === 'server-side error')).toBe(true);
        expect((issues as any).externalIssuesFound).toBeUndefined();
        expect((issues as any).issuesFound).toBeUndefined();
    });

    test('returns the same visible issue set as getIssuesFound()', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        // builder.field('Field1', LookupKey.String).requireText('required');

        let testItem = new ValueHostsManager(vmConfig);
        testItem.getFieldValueHost('Field1')!.setValues('', '');
        testItem.validate();

        let externalIssue: IssueFound = {
            errorMessage: 'server-side error'
        };

        let payload = testItem.toValidationPayload([externalIssue]);
        let payloadIssues = JSON.parse(payload) as IssueFound[] | null;
        let visibleIssues = testItem.getIssuesFound();

        expect(payloadIssues).toEqual(visibleIssues);
    });
});

describe('fromValidationPayload()', () => {
    test('returns false for null payload', () => {
        let setup = setupValueHostsManagerForAddExternalIssueFoundTests();

        let result = setup.valueHostsManager.fromValidationPayload('null');

        expect(result).toBe(false);
    });

    test('clears existing external issues before import', () => {
        let config = {
            ...createValidatableValueHostBaseConfig(1),
            name: 'Field1',
            label: 'Field 1'
        };

        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);

        setup.valueHostsManager.addExternalIssueFound(
            {
                valueHostName: 'Field1',
                errorMessage: 'OLD_EXTERNAL'
            },
            true
        );

        let sourceSetup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);
        sourceSetup.valueHostsManager.addExternalIssueFound(
            {
                valueHostName: 'Field1',
                errorMessage: 'NEW_EXTERNAL'
            },
            true
        );

        let payload = sourceSetup.valueHostsManager.toValidationPayload(null);

        let result = setup.valueHostsManager.fromValidationPayload(payload);
        let issuesFound = setup.valueHostsManager.getIssuesFound();

        expect(result).toBe(true);
        expect(issuesFound).not.toBeNull();
        expect(issuesFound?.some((x) => x.errorMessage === 'OLD_EXTERNAL')).toBe(false);
        expect(issuesFound?.some((x) => x.errorMessage === 'NEW_EXTERNAL')).toBe(true);
    });

    test('preserves existing validator-managed issues while importing external issues', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        // builder.field('Field1', LookupKey.String).requireText('required');
        // let valConfig = builder.complete();

        let target = new ValueHostsManager(vmConfig);
        target.getFieldValueHost('Field1')!.setValues('', '');
        target.validate();

        let source = new ValueHostsManager(vmConfig);
        source.addExternalIssueFound(
            {
                errorMessage: 'IMPORTED_MODEL_ERROR'
            },
            true
        );

        let payload = source.toValidationPayload(null);

        let result = target.fromValidationPayload(payload);
        let issuesFound = target.getIssuesFound();

        expect(result).toBe(true);
        expect(issuesFound).not.toBeNull();
        expect(issuesFound?.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required')).toBe(true);
        expect(issuesFound?.some((x) => x.errorMessage === 'IMPORTED_MODEL_ERROR')).toBe(true);
    });

    test('round-trips a field-level validator-generated issue via toValidationPayload(); existing validator overrides external resulting in issueFound from the validator with doNotSave=true', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        // builder.field('Field1', LookupKey.String).requireText('required');
        // let valConfig = builder.complete();

        let source = new ValueHostsManager(vmConfig);
        source.getFieldValueHost('Field1')!.setValues('', '');
        source.validate();

        let payload = source.toValidationPayload(null);

        let target = new ValueHostsManager(vmConfig);
        let result = target.fromValidationPayload(payload);
        let issuesFound = target.getIssuesFound();

        expect(result).toBe(true);
        expect(issuesFound).not.toBeNull();

        let importedIssue = issuesFound!.find((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required');
        expect(importedIssue).toBeDefined();
        expect(importedIssue!.doNotSave).toBe(true);    // because we've switched to the Validator's IssueFound
    });

    test('round-trips a model-level external issue via toValidationPayload()', () => {
        let source = setupValueHostsManagerForAddExternalIssueFoundTests();
        source.valueHostsManager.addExternalIssueFound(
            {
                errorMessage: 'MODEL_ERROR'
            },
            true
        );

        let payload = source.valueHostsManager.toValidationPayload(null);

        let target = setupValueHostsManagerForAddExternalIssueFoundTests();
        let result = target.valueHostsManager.fromValidationPayload(payload);
        let issuesFound = target.valueHostsManager.getIssuesFound();

        expect(result).toBe(true);
        expect(issuesFound).not.toBeNull();

        let importedIssue = issuesFound!.find((x) => x.errorMessage === 'MODEL_ERROR');
        expect(importedIssue).toBeDefined();
        expect(importedIssue!.valueHostName).toBe(ModelValidatorsValueHostName);
        expect(importedIssue!.doNotSave).toBe(false);
    });

    test('imports a server issue for a field missing on the client by routing it to ModelValidatorsValueHost', () => {
        let serverServices = createJivsServicesForTesting();
        let serverConfig = <ValueHostsManagerConfig>{ services: serverServices, valueHostConfigs: [] };
        serverConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required 1',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });
        serverConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field2',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required 2',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });


        // let serverBuilder = createConfigBuilder(serverServices);
        // serverBuilder.field('Field1', LookupKey.String).requireText('required 1');
        // serverBuilder.field('Field2', LookupKey.String).requireText('required 2');
        // let serverConfig = serverBuilder.complete();

        let source = new ValueHostsManager(serverConfig);
        source.getFieldValueHost('Field1')!.setValues('', '');
        source.getFieldValueHost('Field2')!.setValues('', '');
        source.validate();

        let payload = source.toValidationPayload(null);

        let clientServices = createJivsServicesForTesting();
        let clientConfig = <ValueHostsManagerConfig>{ services: clientServices, valueHostConfigs: [] };
        clientConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required 1',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });


        // let clientBuilder = createConfigBuilder(clientServices);
        // clientBuilder.field('Field1', LookupKey.String).requireText('required 1');
        // let clientConfig = clientBuilder.complete();

        let target = new ValueHostsManager(clientConfig);
        let result = target.fromValidationPayload(payload);
        let issuesFound = target.getIssuesFound();

        expect(result).toBe(true);
        expect(issuesFound).not.toBeNull();

        let field1Issue = issuesFound!.find((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required 1');
        let missingFieldIssue = issuesFound!.find((x) => x.errorMessage === 'required 2');

        expect(field1Issue).toBeDefined();
        expect(field1Issue!.doNotSave).toBe(true); // swapped to client validator issue

        expect(missingFieldIssue).toBeDefined();
        expect(missingFieldIssue!.valueHostName).toBe(ModelValidatorsValueHostName);
        expect(missingFieldIssue!.doNotSave).toBe(false); // imported and not swapped
    });

    test('keeps imported doNotSave = false when the client has the field but no matching validator to swap with', () => {
        let serverServices = createJivsServicesForTesting();
        let serverConfig = <ValueHostsManagerConfig>{ services: serverServices, valueHostConfigs: [] };
        serverConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        // let serverBuilder = createConfigBuilder(serverServices);
        // serverBuilder.field('Field1', LookupKey.String).requireText('required');
        // let serverConfig = serverBuilder.complete();

        let source = new ValueHostsManager(serverConfig);
        source.getFieldValueHost('Field1')!.setValues('', '');
        source.validate();

        let payload = source.toValidationPayload(null);

        let clientServices = createJivsServicesForTesting();
        let clientConfig = <ValueHostsManagerConfig>{ services: clientServices, valueHostConfigs: [] };
        clientConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: []
        });


        // let clientBuilder = createConfigBuilder(clientServices);
        // clientBuilder.field('Field1', LookupKey.String);
        // let clientConfig = clientBuilder.complete();

        let target = new ValueHostsManager(clientConfig);
        let result = target.fromValidationPayload(payload);
        let issuesFound = target.getIssuesFound();

        expect(result).toBe(true);
        expect(issuesFound).not.toBeNull();

        let importedIssue = issuesFound!.find((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required');
        expect(importedIssue).toBeDefined();
        expect(importedIssue!.doNotSave).toBe(false); // no client validator issue to swap with
    });

    test('forces imported doNotSave = false', () => {
        let payload = JSON.stringify([
            {
                valueHostName: 'Field1',
                errorMessage: 'SERVER_ERROR',
                doNotSave: true
            } satisfies IssueFound
        ]);

        let config = {
            ...createValidatableValueHostBaseConfig(1),
            name: 'Field1',
            label: 'Field 1'
        };

        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);

        let result = setup.valueHostsManager.fromValidationPayload(payload);
        let issuesFound = setup.valueHostsManager.getIssuesFound();

        expect(result).toBe(true);
        expect(issuesFound).not.toBeNull();

        let importedIssue = issuesFound!.find((x) => x.valueHostName === 'Field1' && x.errorMessage === 'SERVER_ERROR');
        expect(importedIssue).toBeDefined();
        expect(importedIssue!.doNotSave).toBe(false);
    });

    test('applies encode callback when provided', () => {
        let source = setupValueHostsManagerForAddExternalIssueFoundTests();
        source.valueHostsManager.addExternalIssueFound(
            {
                errorMessage: '<b>MODEL_ERROR</b>'
            },
            true
        );

        let payload = source.valueHostsManager.toValidationPayload(null);

        let target = setupValueHostsManagerForAddExternalIssueFoundTests();
        let result = target.valueHostsManager.fromValidationPayload(payload, (text) => `encoded:${text}`);
        let issuesFound = target.valueHostsManager.getIssuesFound();

        expect(result).toBe(true);
        expect(issuesFound).not.toBeNull();

        let importedIssue = issuesFound!.find((x) => x.errorMessage === 'encoded:<b>MODEL_ERROR</b>');
        expect(importedIssue).toBeDefined();
    });

    test('imports multiple issues from one payload', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        // let vmConfig = { services: services, valueHostConfigs: [] };
        // builder.field('Field1', LookupKey.String).requireText('required');
        // let valConfig = builder.complete();
        let source = new ValueHostsManager(vmConfig);
        source.getFieldValueHost('Field1')!.setValues('', '');
        source.validate();
        source.addExternalIssueFound(
            {
                errorMessage: 'MODEL_ERROR'
            },
            true
        );

        let payload = source.toValidationPayload(null);

        let target = new ValueHostsManager(vmConfig);
        let result = target.fromValidationPayload(payload);
        let issuesFound = target.getIssuesFound();

        expect(result).toBe(true);
        expect(issuesFound).not.toBeNull();
        expect(issuesFound!.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required')).toBe(true);
        expect(issuesFound!.some((x) => x.errorMessage === 'MODEL_ERROR')).toBe(true);
    });
});

describe('clearExternalIssuesFound()', () => {
    test('clears existing external issues', () => {
        let setup = setupValueHostsManagerForAddExternalIssueFoundTests();

        setup.valueHostsManager.addExternalIssueFound(
            {
                errorMessage: 'MODEL_ERROR'
            },
            true
        );

        expect(setup.valueHostsManager.getIssuesFound()?.some((x) => x.errorMessage === 'MODEL_ERROR')).toBe(true);

        setup.valueHostsManager.clearExternalIssuesFound();

        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });

    test('does not clear validator-managed issues', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        // let vmConfig = { services: services, valueHostConfigs: [] };
        // builder.field('Field1', LookupKey.String).requireText('required');
        // let valConfig = builder.complete();

        let testItem = new ValueHostsManager(vmConfig);
        testItem.getFieldValueHost('Field1')!.setValues('', '');
        testItem.validate();

        expect(testItem.getIssuesFound()?.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required')).toBe(true);

        testItem.clearExternalIssuesFound();

        expect(testItem.getIssuesFound()?.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required')).toBe(true);
    });

    test('clears external issues while preserving validator-managed issues', () => {
        let services = createJivsServicesForTesting();
        let vmConfig = <ValueHostsManagerConfig>{ services: services, valueHostConfigs: [] };
        vmConfig.valueHostConfigs.push(<FieldValueHostConfig>{
            valueHostType: ValueHostType.Field,
            name: 'Field1',
            dataType:  LookupKey.String,
            validatorConfigs: [{
                errorMessage: 'required',
                conditionConfig: {
                    conditionType: ConditionType.RequireText
                }
            }]
        });

        // let vmConfig = { services: services, valueHostConfigs: [] };
        // builder.field('Field1', LookupKey.String).requireText('required');
        // let valConfig = builder.complete();

        let testItem = new ValueHostsManager(vmConfig);
        testItem.getFieldValueHost('Field1')!.setValues('', '');
        testItem.validate();
        testItem.addExternalIssueFound(
            {
                errorMessage: 'MODEL_ERROR'
            },
            true
        );

        let issuesBeforeClear = testItem.getIssuesFound();
        expect(issuesBeforeClear?.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required')).toBe(true);
        expect(issuesBeforeClear?.some((x) => x.errorMessage === 'MODEL_ERROR')).toBe(true);

        testItem.clearExternalIssuesFound();

        let issuesAfterClear = testItem.getIssuesFound();
        expect(issuesAfterClear?.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'required')).toBe(true);
        expect(issuesAfterClear?.some((x) => x.errorMessage === 'MODEL_ERROR')).toBe(false);
    });

    test('clears field-level and model-level external issues', () => {
        let config = {
            ...createValidatableValueHostBaseConfig(1),
            name: 'Field1',
            label: 'Field 1'
        };

        let setup = setupValueHostsManagerForAddExternalIssueFoundTests([config]);

        setup.valueHostsManager.addExternalIssueFound(
            {
                valueHostName: 'Field1',
                errorMessage: 'FIELD_ERROR'
            },
            true
        );
        setup.valueHostsManager.addExternalIssueFound(
            {
                errorMessage: 'MODEL_ERROR'
            },
            true
        );

        let issuesBeforeClear = setup.valueHostsManager.getIssuesFound();
        expect(issuesBeforeClear?.some((x) => x.valueHostName === 'Field1' && x.errorMessage === 'FIELD_ERROR')).toBe(true);
        expect(issuesBeforeClear?.some((x) => x.errorMessage === 'MODEL_ERROR')).toBe(true);

        setup.valueHostsManager.clearExternalIssuesFound();

        expect(setup.valueHostsManager.getIssuesFound()).toBeNull();
    });

});

